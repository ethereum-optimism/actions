# Production-Readiness Review — Findings Ledger

Deduped index of all findings across the 17 review passes. Each pass appends new unique findings here and writes its full detail to `docs/reviews/review-pass-NN.md`. Tickets are synthesized from this ledger at the end (`docs/tickets/`).

**Standing rules:** DeFi codebase, dominant risk = user **loses funds** or **signs a malicious/unintended tx**. Findings flag *missing obvious validation* (a malformed call, params that don't match what was explicitly passed, or a validation present in one provider/action but absent in a sibling) — not speculative intent-guessing or broad refuse-to-sign. RPC-trust is an accepted/documented assumption (integrators supply their own RPC). Permit2 signature payloads count as signing-path surface. SDK refactor recommendations are allowed; demo/CLI are review-only.

**Severity:** critical | high | medium | low. **Class:** fund-loss | malicious-sign | correctness | infra | info.

**Status legend:** `new` · `refines:Fxxx` · `dup:Fxxx` · `verified` · `demoted` (failed adversarial verification).

`NEXT_ID: F344`

| ID | Pass | Surface | File:Line | Severity | Class | Title | Status | Cand. issue |
|----|------|---------|-----------|----------|-------|-------|--------|-------------|
<!-- merge agents append rows below this line -->
| F334 | 17 | cli | packages/cli/src/utils/parseAmount.ts:20-28; float round-trip parseDecimal.ts:23 | medium | correctness | CLI amount-parse layer is asset-agnostic: parseAmount guards positivity (value<=0) and integer magnitude (>MAX_SAFE_INTEGER) but NEVER the fractional-digit count vs the resolved asset's decimals, though decimals ARE available at the same call site (swap util.ts:148-151, lend runLendAction.ts:64-80, borrow open.ts:21-25). SDK adds no decimal-precision check before parseUnits(amount, asset.decimals), and parseDecimal returns Number(raw) so >~15-16 significant digits are float-rounded before the SDK re-parses. viem parseUnits silently ROUNDS excess fractional digits (e.g. --amount-in 0.0000001 on 6-dec USDC -> 0 raw) rather than throwing. Worse, lend/borrow envelopes echo the user's TYPED number (runLendAction.ts:96-107, open.ts:40-43, runBorrowAction.ts:143) not the rounded on-chain amount, so an agent reading --json sees the requested value not what moved; only the swap envelope prints the SDK's actual amountIn. Input-side analogue of the refines:F252 serializer type-lie | new | #419 |
| F335 | 17 | cli | packages/cli/src/utils/parseSlippage.ts:9-15 | low | correctness | --slippage 0 passes CLI+SDK validation and is honored as a strict 0% tolerance, NOT coerced to default: parseSlippage delegates to parseDecimal (accepts 0) and returns 0/100=0, which is !==undefined so buildSwapInputs (util.ts:160) forwards slippage:0; SDK resolves via params.slippage ?? defaultSlippage (SwapProvider.ts:271,449,461) so 0 is taken literally and validateSlippage(0,max) passes (validation.ts:111-113 rejects only <0/>max). Result amountOutMin==amountOut (zero protective band): any block-to-block drift reverts on-chain (wasted gas, opaque exit) instead of a clean CLI error, indistinguishable from a flag-omission typo; sibling parseAmount.ts:22 rejects <=0 so also an inter-sibling validation gap. Slippage analog of F330's deadline-bounds lower-extreme gap | new | none |
| F336 | 17 | cli | packages/cli/src/utils/parseAmount.ts:1-28 (and parseDecimal/parseSlippage/parseDeadline/parseApprovalMode) | low | info | Zero unit tests cover the CLI parse layer that gates every fund-moving amount/slippage/signed-deadline into the SDK (grep for parseAmount/parseDecimal/parseSlippage/parseApprovalMode/parseDeadline in packages/cli/src returns no test files; only util test is receipts.test.ts). These are the entire validation boundary between commander argv and signed SDK params (MAX_SAFE_INTEGER/positivity guards, scientific-notation/hex/sign/whitespace rejection, percent->decimal, positive-integer deadline); a regression accepting a negative amount / mis-converting slippage / dropping the magnitude guard would silently change the signed value and nothing fails. Command __tests__ exercise only read-only market/quote verbs, never a write verb's amount-parse path | new | #335 |
| (refines:F333) | 17 | cli | packages/cli/src/commands/wallet/lend/runLendAction.ts:76-80 | low | correctness | lend close --max on a zero/empty position throws a misleading `Invalid --amount: 0` naming a flag the user never passed: runLendAction reads getPosition().balanceFormatted and routes the '0' string through parseAmount with NO flag arg, so parseAmount uses its default '--amount' label and rejects via value<=0; the user invoked close --max and never supplied --amount, masking the real condition (nothing to close). Same snapshot-balance mechanism as F333, distinct error-clarity gap; borrow --max siblings avoid it via the SDK {max:true} sentinel. Fix = detect empty/zero position before parseAmount and surface a clear CliError | refines:F333 | #334 |
| (refines:F333) | 17 | cli | packages/cli/src/commands/wallet/lend/runLendAction.ts:76-80 | low | correctness | lend close --max re-derives the withdraw amount from a float-formatted balance (Number(balanceFormatted) via parseDecimal.ts:23), so 'max' leaves wei-scale dust and is not a true full-balance path: unlike borrow's {max:true} AmountOrMax that resolves on-chain at dispatch, the SDK closePosition (LendProvider.ts:195-227) only accepts a human amount re-parsed via parseUnits, and the float round-trip on an 18-dec balance drops low-order wei -> under-withdraw leaves a residual position. Direction is safe (no overdraw), correctness/dust not fund-loss; the inflight-interest race is documented (71-75) but the float-precision dust is a separate undocumented effect. Adds the float-dust mechanism to F333's snapshot-vs-{max:true} parity gap | refines:F333 | #379 |
| F337 | 17 | cli | packages/cli/src/commands/wallet/borrow/runBorrowAction.ts:81-88 | low | info | amountOrMaxToEnvelope handles only {max} and {amount} of the AmountOrMax union and returns undefined for {amountRaw} (line 87, comment 'the CLI never builds it'); holds today (every borrow handler builds {amount}/{max}) so not exploitable, but it is the single projection from the SDK union into the user-facing envelope and if a future leg ever passes {amountRaw} (the SDK type permits it) the borrow receipt (printOutput.ts:238-242) prints NO amount for a fund-moving leg while the tx still dispatches the raw amount -- latent silent observability hole on a write verb's confirmation output. Fix = explicit `if ('amountRaw' in value) return value.amountRaw.toString()` branch | new | none |
| F338 | 17 | cli | packages/cli/src/output/errors.ts:181-191,219-252 | medium | info | Redactor SENSITIVE_KEYS denylist (account/address/from/headers/privateKey/publicKey/request/signer/signature) is name-keyed only; any OTHER key whose value is a string is KEPT after only redactUrls (strips http(s), never high-entropy hex) -- proven by errors.test.ts:131 preserving unknownScalar:'drop-me'. toCliError stashes the RAW caught error as details:{cause:err} on onchain (130) and network (133) paths (also wallet/balance.ts:27); when err is NOT a viem BaseError, redactRecord recurses ALL enumerable props. SENSITIVE_KEYS omits transaction/rawTransaction/serializedTransaction/data/value/to/params/body/args/mnemonic/seed, so a thrown error carrying any of those as a string (raw signed tx, calldata, seed phrase from a wrapped/non-viem signing path) lands verbatim in stderr and the --json error envelope agents/CI log. General form of F328 (the specific parseSigner reason instance), structural inverse of F332. Fix = value-aware redaction (drop 0x[0-9a-fA-F]{40,} / BIP39 regardless of key) + add the missing keys | new | none |
| F339 | 17 | cli | packages/cli/src/output/errors.ts:197-252,309 | low | correctness | redactValue/redactRecord recurse into nested objects/arrays with NO depth cap and NO WeakSet cycle guard; viem BaseError is flattened to {errorName,shortMessage} (safe) but toCliError (130,133) and wallet/balance.ts:27 stash arbitrary thrown values under details:{cause:err}, and a non-BaseError thrown value with a circular reference (common when an error attaches a back-referencing request/response/transaction) recurses until RangeError: Maximum call stack size exceeded. This throws inside safeDetails during writeError (309), the process-level last-resort unhandledRejection/uncaughtException sink (index.ts:20-24,60), so the original failure is masked and the process exits on an unhandled stack overflow instead of emitting the structured envelope. Fix = depth limit + WeakSet returning [truncated]/[circular] | new | none |
| F340 | 17 | cli | packages/cli/src/config/env.ts:13-49 | low | info | requireEnv('PRIVATE_KEY') reads from the lazily-populated module-level cache:CliEnv (13-22) which holds the raw private-key string for the entire subprocess lifetime; walletContext keeps it flowing through parseSigner (walletContext.ts:39) and the cached string is never cleared after privateKeyToAccount consumes it, so the secret stays resident and is reachable by anything later reading process.env.PRIVATE_KEY or the cache closure (core dump, heap snapshot, debug print of process.env). Acceptable for the short-lived subprocess model (documented env.ts:25-30), recorded info-only; noted alongside the redactor findings since it is the value the redactor must never echo. If a long-lived/REPL mode lands (#411/#422), zero the cached key after signer construction | new | none |
| F341 | 17 | cli | packages/cli/src/output/errors.ts:234-237 (string branch),167-179 (SCALAR_ALLOWLIST),259-261 (docstring) | low | info | In redactRecord the `typeof raw==='string'` branch (234-237) runs BEFORE the SCALAR_ALLOWLIST gate (238-241), so every string-valued detail whose key is not in SENSITIVE_KEYS is preserved with only redactUrls applied; the allowlist only ever gates non-string scalars (number/boolean/bigint, 242-249). This is the structural root of F328: parseSigner's reason:cause.message (walletContext.ts:22) leaks because the string branch is a global passthrough, NOT because reason is allowlisted -- the allowlist is irrelevant for strings. The docstring at 259-261 ('unknown scalars preserved only when key in SCALAR_ALLOWLIST') is inaccurate. Fix = deny-list string VALUES by shape (0x[0-9a-f]{40,}/long-hex/bearer) or invert to drop-unless-allowlisted for strings too; at minimum correct the docstring | new | none |
| (refines:F332) | 17 | cli | packages/cli/src/output/errors.ts:234-237,167-191 | low | info | Partial refutation of F332: F332 claims recipient/to/spender are stripped (not in SCALAR_ALLOWLIST) and recommends adding them to a non-secret allowlist, but verified against code + standalone repro of redactRecord, address-typed to/recipient/spender are STRING values so they hit the string branch (234-237) and pass through (URL-strip only) BEFORE the SCALAR_ALLOWLIST check; preserved today in their normal string form. F332's gap only manifests if such a field were a non-string scalar, which addresses never are. Inverse holds: from/account/signer/address ARE dropped (SENSITIVE_KEYS) even as strings (correct). So F332's 'add to allowlist' action is moot for the common case; the destination-context preservation it wants is already in place | refines:F332 | none |
| F342 | 17 | cli | packages/cli/src/resolvers/assets.ts:27-39 | low | correctness | resolveAsset uses allow.find(...) (line 29) and returns the FIRST asset whose metadata.symbol matches case-insensitively, while sibling resolveMarket (markets.ts:44-56) and resolveBorrowMarket (borrowMarkets.ts:48-60) deliberately throw CliError('validation','Ambiguous ...') on duplicate-key collisions (comment: 'the agent would otherwise silently pick whichever appears first in iteration order'). resolveAsset has no ambiguity guard and no ambiguity test (assets.test.ts covers exact/case-insensitive/unknown/empty only); it feeds the fund-moving swap --in/--out path via buildSwapInputs -> resolveAsset (commands/actions/swap/util.ts:148-149) building the signed WalletSwapParams. Config-agnostic resolver: a config with two same-symbol assets (mock USDC_DEMO + real USDC, or same symbol on two chains) makes --in USDC silently select the first-listed token address -- a different ERC-20 than intended, built and signed against it. Sibling-parity gap. Fix = collect all matches, throw on >1 | new | none |
| F343 | 17 | cli | packages/cli/src/resolvers/chains.ts:70-89 | low | info | resolveChainId parses via Number(raw) then Number.isInteger&&>0 (74-75), accepting forms the deliberately-strict amount parsers reject: 0x14a34 (hex->84532), 8.4532e4 (scientific), 84532.0, ' 84532' (whitespace), +84532, 084532 all pass; parseDecimal/parseAmount route through viem parseUnits and parseDeadline enforces /^[1-9]\d*$/. No fund-safety impact: the parsed value is membership-checked against configuredChainIds (82, tight allowlist) and a hex/scientific form resolving to a configured id resolves to the SAME SupportedChainId. Strictness-consistency observation that could mask a typo; fix = gate on /^[1-9]\d*$/ before Number() for parity. Allowlist already prevents any wrong-chain selection | new | none |
| (refines:F327) | 17 | cli | packages/cli/src/output/printOutput.ts:330-335 (formatSwapExecute); SwapExecuteDoc 88-99; dispatch execute.ts:42-53 | low | info | swap execute success envelope omits the resolved recipient even though --recipient can redirect output: formatSwapExecute and SwapExecuteDoc never include a recipient field and execute.ts:42-53 does not read result.recipient into the doc; when the operator passes --recipient (wallet/swap/index.ts:38-40) the executed swap can send output to a non-wallet address on providers that honor it, yet neither stdout nor --json echoes the resolved destination. Post-send half of the recipient-visibility gap (F327 covered the pre-send passthrough/echo + SDK-validation no-op); an agent parsing the success envelope has no machine-readable confirmation of where funds went. Fix = add result.recipient (already on SwapQuote/execute result) to SwapExecuteDoc and formatSwapExecute | refines:F327 | #435 |
| F327 | 16 | cli | packages/cli/src/commands/actions/swap/util.ts:118-128 (buildWalletExecuteParams); flag wallet/swap/index.ts:37-40; dispatch wallet/swap/execute.ts:33-53; output printOutput.ts:88-99,312-320,330-335 | medium | malicious-sign | `wallet swap execute --recipient` is cast raw to WalletSwapParams['recipient'] (util.ts:121, comment defers all validation to SDK) with NO CLI-side isAddress/non-empty/zero-address precheck, AND the resolved recipient is never echoed before or after signing (formatSwapExecute/SwapExecuteDoc and read-only formatSwapQuote omit it though SwapQuote.recipient exists); the SDK guard (validateRecipient) is a no-op for any non-isAddress value so 0xDEADBEEF/typo'd-checksum/non-ENS bypasses into the ENS verbatim-hex branch (no EIP-55 checksum, no zero guard, F043), Uniswap V4 + Velodrome universal/CL silently ignore it (output to signer wallet, F046/#444) while Velodrome v2/leaf bake it into calldata (F079) so a poisoned value silently sends output to the attacker; one sub-reviewer rated the Velodrome-advertises-but-ignores angle high/fund-loss (baked demo config enables Velodrome, exit-0 success envelope). CLI half of the recipient family, only recipient passthrough in the CLI, zero compensating control | new | #444 |
| F328 | 16 | cli | packages/cli/src/context/walletContext.ts:15-25 (parseSigner); redactor output/errors.ts:176,181-191,219-252 | medium | malicious-sign | On a malformed PRIVATE_KEY, parseSigner throws CliError('config', ..., { reason: cause.message }) (walletContext.ts:22); viem 2.x echoes the offending input value in hex/bytes parse errors, and the redactor's string branch applies ONLY redactUrls (strips http(s), never high-entropy hex) while `reason` is in SCALAR_ALLOWLIST and NOT SENSITIVE_KEYS (which keys off field NAME), so a malformed key value can land verbatim in stderr and the --json error envelope that agents/CI capture to logs; only secret the CLI touches crossing into formatted output; defense-in-depth today (current viem describes shape not bytes) but redactor structurally does not defend the value across viem/3rd-party versions; relates F296 (backend env secret-validation gap, distinct surface/mechanism) | new | none |
| F329 | 16 | cli | packages/cli/src/commands/wallet/swap/execute.ts:38-53 (runWalletSwapExecute); borrow/runBorrowAction.ts:127-149; lend/runLendAction.ts:76-93 | low | info | No pre-send echo / --dry-run / --yes / confirmation gate before ANY fund-moving verb (swap execute, lend/borrow open/close/repay/deposit/withdraw); every write resolves flags and immediately calls the SDK sign+broadcast method, printing the receipt only AFTER the tx lands (grep for confirm/dry-run/readline/prompt returns nothing); no pre-send summary of resolved from/recipient/chain/market/amount/approvalMode/deadline, approvalMode silently inherits SDK/wallet default; deliberate non-interactive agent design but a mis-parsed --chain/--market/--amount/--recipient or `max` approval commits on-chain with no checkpoint; natural home to surface F327's resolved recipient; maps to CLI guardrails backlog | new | #414 |
| F330 | 16 | cli | packages/cli/src/commands/actions/swap/util.ts:99-109 (parseDeadline); forwarded WalletSwapParams.deadline util.ts:122 | low | correctness | parseDeadline enforces only /^[1-9]\d*$/ (positive integer) and returns Number(raw), forwarding it as the signed swap deadline with NO future/window/units check: (a) a PAST timestamp (--deadline 1) is encoded and the router reverts on-chain (wasted gas, opaque exit 5) instead of a clean CLI validation error; (b) a 13-digit MILLISECOND value (common Date.now() mistake, flag help says 'seconds') becomes a ~year-33000 deadline that fails OPEN, silently neutering the deadline's staleness/MEV protection so the swap can sit pending and execute at a much-moved price; the only numeric swap flag not sanity-bounded (parseAmount already guards magnitude) | new | none |
| F331 | 16 | cli | packages/cli/src/output/printOutput.ts:132-141 (formatChains); JSON source commands/actions/chains.ts:13-18; redactor output/errors.ts:79-84 | low | info | `actions chains [--json]` prints rpcUrls verbatim (formatChains writes rpc=${row.rpcUrls.join(',')}, JSON emits rpcUrls unredacted) while the error pipeline strips every http(s) URL because they 'frequently embed API keys' (redactUrls); BASE_SEPOLIA_RPC_URL/OP_SEPOLIA_RPC_URL routinely carry an Alchemy/Infura key in the path and are printed in cleartext on the read-only happy path most likely to be piped to a log/file/share; inconsistent with the error-path redaction policy; fix = redact credential portion or print rpcOverride bool + host only | new | none |
| F332 | 16 | cli | packages/cli/src/output/errors.ts:181-252 (safeDetails/SENSITIVE_KEYS/SCALAR_ALLOWLIST/redactRecord) | low | info | safeDetails drops SENSITIVE_KEYS (account/address/from/signer) and only preserves unknown scalars whose key is in SCALAR_ALLOWLIST; on a write verb that fails after partial execution the redactor strips address/from and does NOT preserve recipient/to/spender/marketId (none allowlisted) — exactly the non-secret destination fields an operator needs to confirm WHERE an approval/transfer was targeted in a fund-safety post-mortem; correctly conservative for secrets but the same allowlist suppresses destination context; fix = add recipient/to/spender/marketId/transactionHash/userOpHash to a non-secret allowlist, keep from/account/signer redacted | new | none |
| F333 | 16 | cli | packages/cli/src/commands/wallet/lend/runLendAction.ts:76-93; sibling wallet/borrow/runBorrowAction.ts (resolveAmountOrMax -> {max:true}) | low | correctness | `lend close --max` reads wallet.lend.getPosition().balanceFormatted, parseAmount's it, and dispatches closePosition with that fixed amount (comment acknowledges it races inflight interest accrual so it under-shoots and leaves dust, fail-safe no overdraw); the borrow --max siblings (repay/withdraw/close) instead pass the SDK's {max:true} sentinel that resolves on-chain at dispatch and avoids dust — so lend --max and borrow --max use DIFFERENT mechanisms for the same user-facing --max intent (lend snapshots a formatted balance, borrow defers to SDK); validation/parity gap (sibling does it the safer on-chain-resolve way), not fund-loss; fix = route lend close --max through an SDK full-balance path if exposed | new | #334 |
| (refines:F252) | 16 | cli | packages/cli/src/output/json.ts:14-16 (writeJson); output/errors.ts:304 (writeError) | low | correctness | CLI is the primary production consumer of the serializeBigInt boundary: writeJson/writeError route every success doc and error envelope through serializeBigInt before JSON.stringify, so the documented data-loss (Map/Set->{}, Date->string, undefined dropped) and type-lie (bigint->string while return type claims T) land on the agent-output boundary — an agent parsing --json receives amountInRaw/amountOutRaw/raw balances as decimal STRINGS and silently misbehaves on fund-moving amounts if it assumes numbers; no new CLI-side fix beyond #419, re-verify CLI envelopes round-trip when the SDK serializer is hardened/typed (frontend deserialize side is F325) | refines:F252 | #419 |
| F314 | 15 | frontend | packages/demo/frontend/src/hooks/useBorrowTransaction.ts:99-102 (repay dispatch); BorrowAction.tsx:147,183-187; useBorrowQuotePreview.ts:69 | medium | correctness | Borrow Repay-full never uses the SDK's dust-free `{ max: true }` close (params.ts:59-62, internalParams.ts:138); handleMax sets amount=borrowAmountFormatted (exact-debt string, comment "exact string avoids dust") but BorrowAction.tsx:147 parseFloat(amount)→amountNum drops precision and runTransaction/preview always submit `{ amount: amountNum }` (a fixed float) even when canRepayFull, so a full repay computed at review time is always below the interest-accrued debt at execution and leaves residual dust debt (trips DEBT_DUST_THRESHOLD/repay-gate); not a fund-loss (SDK floors at outstanding debt) but defeats intent-to-close, distinct from F310 (collateral params on the top-up path) | new | #427 |
| F315 | 15 | frontend | packages/demo/frontend/src/components/earn/borrow/BorrowActionModals.tsx:78-90; BorrowAction.tsx:316,332-348 | low | info | Borrow review modal renders the LIVE (non-snapshotted) health/amount/amountUsd/activeAsset computed every render in BorrowAction (268-281), not a snapshot frozen at setReviewModalOpen(true); while the modal is open the debounced useBorrowQuotePreview can resolve/flip isPreviewLoading and currentCollUsd/projectionCollateralUsd can change if borrowPositions/tokenBalances refetch, mutating the projected LTV/health-factor/warning band after the user opened it to review; runTransaction dispatches live amountNum, user cannot edit amount under the modal so not directly exploitable but reviewed-vs-confirmed projection not guaranteed identical (lifecycle/snapshot angle of F310, projection itself stub-priced per F312/F313) | new | none |
| F316 | 15 | frontend | packages/demo/frontend/src/config/actions.ts:37-39 | low | info | Frontend Morpho lend allowlist uses GauntletUSDCDemo (0x018e22BB..., markets.ts:17) while backend (backend/src/config/actions.ts:39) uses MorphoUSDCLendDemo for the apparently-same vault (address matches MorphoUSDCBorrowOPDemo.collateralToken, markets.ts:66); divergent symbol means a change to one allowlist won't track the other and a reviewer cannot confirm parity by name; separately the frontend chains array (67-92) omits UNICHAIN present in the backend; no fund-safety impact today (SDK-enforced allowlists, only two chains operated) but the two configs are meant to mirror ("matching backend structure") and silently diverged | new | none |
| F317 | 15 | frontend | packages/demo/frontend/src/mutations/useLendPosition.ts:48-75; Action.tsx:178-219,221-249; useWalletBalance.ts:263-296 | low | correctness | Lend/withdraw fund-moving submit's double-submit protection is entirely React async state: handleCtaClick checks isActionDisabled (folds in isLoading useState) and runTransaction setIsLoading(true) only AFTER the check, then calls mutateAsync; React Query does not dedupe concurrent mutateAsync on the same instance and CtaButton has no debounce, so two synchronous taps in one render tick both observe isLoading===false and dispatch open/closePositionMutation twice (two on-chain deposit/withdraw txs); review-modal Confirm has the same exposure (gated only by isExecuting={isLoading}); borrow closes this with a synchronous executingRef (useBorrowTransaction.ts:47,60-61), F307 flagged swap — lend/withdraw is the third sibling missing the guard, moving real user principal | new | none |
| F318 | 15 | frontend | packages/demo/frontend/src/api/actionsApi.ts:108-127,119 | low | correctness | lendMutation builds the POST /lend/position/{action} body with tokenAddress: asset.address[marketId.chainId] and NO fallback, so a missing chain entry yields undefined and JSON.stringify omits the key entirely (body with no tokenAddress); the sibling that resolves the same address for display/gating (useWalletBalance.ts:132-133) falls back to Object.values(market.asset.address)[0], so the two derivations of the same token address can disagree; contained today (allowlisted markets carry their chain address, backend 400s on missing tokenAddress) — fail-closed latency footgun, sibling-validation gap on a fund-moving body builder | new | #303 |
| F319 | 15 | frontend | packages/demo/frontend/src/api/actionsApi.ts:143-156 | low | info | dripEthToWallet sends POST /wallet/eth with a caller-supplied walletAddress in the body and passes NO headers/auth, whereas mintDemoUsdcToWallet (72-85) and lend/borrow/swap mutations all forward auth headers; test-token faucet (no user principal) and the unauth'd backend route is F272, but the frontend asymmetry lets the faucet recipient be specified independently of the authenticated session, so faucet-abuse rate-limiting cannot key off the session on this path | new | none |
| F320 | 15 | frontend | packages/demo/frontend/src/api/actionsApi.spec.ts:22-37,39-55 | low | info | Both API-client error-handling tests wrap the awaited call in try/catch and put expect(...) assertions ONLY inside catch, with no expect.assertions(n) and no fail() in try; if a refactor made BaseApiClient.request stop throwing on non-2xx (swallow + return), the catch body never runs and the test still passes green, hiding a regression in the error-propagation contract the lend/swap/borrow mutation error-surfacing (and F305-style revert-reason display) depends on; test-quality lens (test cannot fail when guarded behavior changes) | new | none |
| F321 | 15 | frontend | packages/demo/frontend/src/hooks/useCollateralStatus.ts:26-30 | low | correctness | useCollateralStatus matches the pledged borrow position by `collateralAsset.metadata.symbol === asset.metadata.symbol && asset.address?.[p.marketId.chainId] !== undefined` (symbol + "has some address on the chain"), never comparing the actual on-chain collateral address to the lend asset's address on that chain; positions[0] flows into useWithdrawCollateral → real borrowOperations.withdrawCollateral(marketId: pledgedPosition.marketId, ...); if two configured assets ever share a symbol on a chain the withdraw could release collateral from the wrong borrow market; contained today (unique allowlist symbols) but a symbol-based identity check on a fund-moving path where an address check is available | new | none |
| F322 | 15 | frontend | packages/demo/frontend/src/hooks/useDynamicWallet.ts:25-52 | low | correctness | Smart wallet is created once and never recreated on a signer/account switch: useDynamicWallet guards with `if (isCreating || smartWallet) return` and only resets when primaryWallet becomes null, so a non-null→different-non-null signer transition retains the stale smartWallet and operations/borrowOperations (memoized on it) sign subsequent lend/borrow/swap txs from the previous account; useTurnkeyWallet has the same guard (line 64) and its create-effect deps (line 101) omit httpClient/session/embeddedWallet.accounts[0].address it reads, so a session/org/first-match change does not re-derive the signer; downstream Earn cache reset keys on walletAddress (Earn.tsx:429-434) which also wouldn't fire — signing-path identity that determines whose funds move | new | none |
| F323 | 15 | frontend | packages/demo/frontend/src/hooks/useWithdrawCollateral.ts:122-129 | low | correctness | releaseCollateralAmountRaw converts the underlying withdraw amount to vault shares with CEIL division (numerator = collateralAmountRaw*collateralShares + collateralAmount - 1n; / collateralAmount), releasing up to one share-wei MORE collateral than the underlying withdrawn — opposite of the conservative direction for a collateral release (weakens the borrow position more than the floor-rounded health card projected); the guard only rejects non-positive inputs (122), no clamp to pledgedPosition.collateralShares, so an over-input produces a shares figure above the position balance and relies on the SDK/contract to revert; exceedsDeposit keys off lend maxAmount not pledged collateral, so the two diverge | new | none |
| F324 | 15 | frontend | packages/demo/frontend/src/providers/ActivityLogProvider.tsx:45-52 | low | info | Activity-log storage key is activity-log-${walletAddress} only when truthy, else falls back to activity-log-${walletProvider}/activity-log; server-wallet/Turnkey/Dynamic resolve the address asynchronously, so activity logged in the pre-address window (and the ${key}-next-id counter) is written under the provider-shared key; on a shared machine a second user of the same provider reads the prior user's persisted transaction-typed rows (incl. block-explorer URLs disclosing addresses/tx hashes) until the address resolves and the effect at 80-99 swaps keys; display/privacy only, leaks per-user activity across sessions on the same browser profile | new | none |
| F325 | 15 | frontend | packages/demo/frontend/src/api/borrowApi.serializers.ts:62-75 | low | correctness | deserializeQuote spreads ...q (Serialized<BorrowQuote>) and only re-hydrates top-level bigints (borrowAmountRaw, collateralAmountRaw, gasEstimate, two nested positions), never touching q.execution; BorrowQuote.execution.transactions[].value is statically bigint (quote.ts:82-83) but after JSON is a decimal string, so the returned value typed BorrowQuote carries string-typed value legs — a type-lie on the quote the SDK doc (quote.ts:48-49) says to re-dispatch via wallet.borrow.*; NOT exploitable today (server-wallet path re-executes from raw Stub*Params; quote consumed only by gate/preview reading positionAfter/borrowAmountRaw) but any future in-browser dispatch of the deserialized quote sends malformed/zero value legs with no compile-time warning (frontend deserialize side of the F252/F044 serializer type-lie) | new | #419 |
| F326 | 15 | frontend | packages/demo/frontend/src/demoMagic/ReacquireDebtNotice.tsx:20 | low | info | maxRepayable = Math.min(debtBalance, outstandingDebt) (both parseFloat-derived numbers, BorrowAction.tsx:118) is interpolated raw into "You can repay up to ${maxRepayable} ${symbol}" (ReacquireDebtNotice.tsx:20); the amount input is floored to asset decimals via floorToAsset before submit (BorrowAction.tsx:127-131,208) but this advisory text is not, so a value like 12.3456789012 displays and (being unfloored) can read slightly higher than the floored input would accept; display-only, no tx impact, slightly over-states repayable funds in a fund-state hint | new | none |
| (refines:F302) | 15 | frontend | packages/demo/frontend/src/components/earn/ReviewSwapModal.tsx:88-91,121,135-137 | low | correctness | Sharpens F302: the "Max slippage" DetailRow (88-91) is driven by the SAME slippage prop that defaults to 0.005 (121) and is never passed by SwapAction.tsx:593-606, so both the displayed slippage % and the min-received are independent hardcoded literals; the SwapQuote priceQuote already carries authoritative slippage/amountOutMin/amountOutMinRaw (base.ts:216-219,240-241) the modal ignores, and getQuote is called without a slippage arg (useSwap.ts:68-76, frontendWalletOperations.ts:113-120) so the SDK defaultSlippage (0.005) is what's actually enforced — coinciding with the literals only by accident; an integrator defaultSlippage/maxSlippage override makes the confirm screen display a slippage figure and min-out the executed tx does not enforce | refines:F302 | #435 |
| (refines:F302) | 15 | frontend | packages/demo/frontend/src/components/earn/ReviewSwapModal.tsx:135-137 | low | info | formatSwapAmount is declared (amount: number) (tokenDisplay.ts:110) but ReviewSwapModal.tsx:135-137 passes (parsedOut*(1-slippage)).toFixed(6), a string; it works because the body does String(amount).split('.') but the amount===0 zero-guard (tokenDisplay.ts:114) uses === against a number and never matches a string, so a zero min-received renders "0.000000" not "0"; cosmetic but the type signature is violated and the zero-guard is silently dead on this call path; folds into the F302/#435 fix (pass quote.amountOutMin as a number) | refines:F302 | #435 |
| (refines:F308) | 15 | frontend | packages/demo/frontend/src/demoMagic/aaveDemoMagic.ts:44-71 | low | info | Sharpens F308 with the over-removal arithmetic: on borrow mirrorBorrowReceipt mints receipt.borrowAmount = quote.borrowAmountRaw (principal) USDC_DEMO, but on a full close/repay (frontendWalletOperations.ts:158,167) it removes receipt.borrowAmount which for an Aave close is repay.repayAmount = full outstanding debt INCLUDING accrued interest (aave/quote.ts:278); only principal was ever minted, so after any interest accrual the "remove" transfer (67-72) requests more USDC_DEMO than the wallet holds and reverts; sendMirrorTx swallows the revert (80-88, F308 fire-and-forget) so the UI shows the real close as Confirmed while stale USDC_DEMO lingers and the repay gate (BorrowAction.tsx:109,117) reflects phantom funds; demo-only, fix = cap removal at min(receipt.borrowAmount, balance) | refines:F308 | none |
| F302 | 14 | frontend | packages/demo/frontend/src/components/earn/ReviewSwapModal.tsx:121,135-137 | medium | correctness | Swap Review modal recomputes "Minimum received"/"Max slippage" from a hardcoded 0.005 literal applied to the displayed amountOut (rendered without a slippage prop, SwapAction.tsx:593-606), never reading the SDK quote's authoritative amountOutMin/amountOutMinRaw; the two 0.005 values (modal literal + config defaultSlippage) coincide today but are wired from independent sources, so the confirm-screen protection silently diverges from the on-chain min-out if either changes (frontend-modal layer of F005/#435) | new | #435 |
| F303 | 14 | frontend | packages/demo/frontend/src/components/earn/serverWalletOperations.ts:46-64 | medium | correctness | Server-wallet executeSwap forwards only a float quote.amountIn + provider to POST /swap/execute, omitting slippage/amountInRaw/amountOutMinRaw, so the backend re-quotes independently and the reviewed quote's min-out is never enforced; unlike the frontend-wallet path (wallet.swap.execute(quote)) which executes the exact reviewed quote, leaving the two sibling execution paths with different parity guarantees (actionsApi.executeSwap accepts a slippage field this path never populates) | new | #435 |
| F304 | 14 | frontend | packages/demo/frontend/src/demoMagic/morphoDemoMagic.ts:9-40 | medium | correctness | useReconcileMorphoCollateral auto-submits depositCollateral({max:true}) on mount via a render useEffect for every unpledged Morpho lend position — a real fund-moving max collateral pledge with NO review modal and NO user confirmation, gated only by reconciledRef (a Set that resets on remount/refresh and clears on failure), pledging the user's entire lend-vault share balance and misrepresenting freely-withdrawable shares as pledged | new | none |
| F305 | 14 | frontend | packages/demo/frontend/src/components/earn/SwapAction.tsx:443-446,608-612 | medium | correctness | handleConfirmSwap swallows the revert reason: on failure it only console.errors and renders TransactionModal with NO errorMessage (falls back to "Try again later."), while sibling lend/withdraw (Action.tsx) and borrow (useBorrowTransaction) surface ActionsError.shortMessage; the one fund-moving action most likely to revert for a fund-safety reason (slippage exceeded, insufficient balance, stale quote) is the only one that hides the cause from the user | new | none |
| F306 | 14 | frontend | packages/demo/frontend/src/components/earn/SwapAction.tsx:273-324,387-453 | low | correctness | Swap quote has no staleness/expiry surfacing: SwapQuote.quotedAt/expiresAt/deadline are never read; handleConfirmSwap reuses the stored quote with no recency check or re-fetch on Confirm, so an open Review modal can submit an arbitrarily stale quote (frontend-wallet executes the stale calldata; server-wallet re-quotes so displayed amounts diverge from executed), with no "quote expired, refresh" affordance | new | #435 |
| F307 | 14 | frontend | packages/demo/frontend/src/hooks/useSwap.ts:98-117 | low | correctness | Swap double-submit guard relies on async React state (if (isSwapping) return + disabled={isExecuting}), so two confirms within the same render tick can both pass and dispatch operations.executeSwap twice (spending the input token twice); the sibling borrow path closes this exact window with a synchronous executingRef ref (useBorrowTransaction.ts:47,60-61), making swap the inconsistent sibling | new | none |
| F308 | 14 | frontend | packages/demo/frontend/src/demoMagic/aaveDemoMagic.ts:35-89 | low | info | Frontend Aave USDC_DEMO mirror is fire-and-forget: mirrorBorrowReceipt is invoked as void and sendMirrorTx swallows mint/remove failures into console.error only (never reads the UserOp success flag, no idempotency key), so a failed mirror after a real borrow shows Confirmed while USDC_DEMO never updates (repay gate then claims no balance), and a client retry double-mirrors; user-visible frontend twin of backend F292 | refines:F292 | none |
| F309 | 14 | frontend | packages/demo/frontend/src/api/actionsApi.ts:108-127,236-289 | low | info | Lend (lendMutation) and swap (executeSwap) fund-moving POSTs carry no client idempotency key (same for borrowApi.postMutation); React Query in-flight flags cover double-clicks, but MUTATION_TIMEOUT_MS=30s AbortSignal.timeout (apiClient.ts:33) aborts a slow-but-successful mutation client-side, so a caller retry or reload + re-submit re-executes the on-chain action with no server-side dedup (timeout/retry double-submit window) | new | none |
| F310 | 14 | frontend | packages/demo/frontend/src/hooks/useBorrowQuotePreview.ts:50-64 | low | correctness | Borrow CTA gate validates a preview quote whose collateral params differ from the executed tx on the top-up path: the preview pledges collateralAmount:{amountRaw:directDepositedSharesRaw} while useBorrowTransaction.ts:88-97 sends NO collateralAmount for top-ups, so the /borrow/quote gate (BorrowAction.tsx:236) validates a lower-LTV borrow-with-added-collateral while the executed call adds debt against existing collateral only; contained because the health card uses the local projection, but the gate validates params that don't match the executed call | new | none |
| F311 | 14 | frontend | packages/demo/frontend/src/hooks/useTotalBalance.ts:49-71,89-90 | low | info | Portfolio USD valuation derives a per-token price from a swap quote for amountIn:1 and multiplies by the full balance, so the unit price embeds the 1-unit trade's price impact and the linear extrapolation misrepresents realizable value for larger holdings; price is also cached indefinitely per symbol (no TTL) so the displayed total drifts from market; display-only (no tx built), tracked by #482 | new | #482 |
| F312 | 14 | frontend | packages/demo/frontend/src/utils/borrowValuation.ts:17-32 | low | info | positionUsd multiplies amounts by stubPriceUsd(symbol), which returns 0 for any symbol absent from the hardcoded table (stubPrices.ts:14-18); an unpriced collateral/borrow asset values the position at $0, making computeHealthFactor return Infinity (borrowMath.ts:61) and rendering a position with real debt as never at liquidation risk; fail-open default (0/Infinity) not reachable today (markets allowlisted) but a hazard, tracked by #482 | new | #482 |
| F313 | 14 | frontend | packages/demo/frontend/src/utils/tokenDisplay.ts:56-77 | low | info | deriveUsdRates prices the non-stable leg from the opposite leg of the same swap (usdPerIn = amountOut/amountIn), so the Review modal's "You pay"/"You receive" USD figures are equal by construction (paidUSD == receivedUSD) regardless of price impact; the USD readout can never signal an unfavorable trade; known demo stub, tracked by #482 | new | #482 |
| F283 | 13 | backend | packages/demo/backend/src/controllers/swap.ts:36-43 | low | correctness | swap getQuote PriceRequestSchema transforms amountIn/amountOut via Number() with no positivity/finiteness check (NaN/-5/1e400 all pass) and no exactly-one-of refinement, while sibling ExecuteSwapRequestSchema constrains the same field with z.number().positive(); the unauthenticated quote path forwards malformed numbers into actions.swap.getQuote (input-validation symmetry gap, relates F280) | new | none |
| F284 | 13 | backend | packages/demo/backend/src/controllers/lend.ts:24-29,37-42 | low | correctness | lend open/close marketId.chainId validated as z.number().positive() only, never membership in SUPPORTED_CHAIN_IDS, unlike swap.ts chainIdFromNumber and the allowlist-derived borrow path; an unsupported-but-positive chainId is cast `as SupportedChainId` and reaches resolveAsset/SDK instead of a 400 (relates F281) | new | #334 |
| F285 | 13 | backend | packages/demo/backend/src/utils/assets.ts:15 | low | correctness | resolveAsset does case-sensitive `token.address[chainId] === tokenAddress` against EIP-55 checksum-cased config addresses while swap/lend controllers pass tokenAddress unnormalized and the borrow/wallet AddressSchema lowercases; a supported token sent in lowercase yields 'Asset not found'/500, two address-handling contracts coexist in one backend | new | #475 |
| F286 | 13 | backend | packages/demo/backend/src/controllers/borrow.ts:97-107,117-127,138-148,159-169,178-188 | low | info | borrow/lend/swap mutation handlers run validateRequest BEFORE requireAuth while borrow getQuote (and its own comment) runs auth first; route-level authMiddleware already gates these so no fund-loss, but the file contradicts its stated convention and would give schema 400 before a 401 (gate-ordering inconsistency, relates F274) | new | none |
| F287 | 13 | backend | packages/demo/backend/src/services/wallet.ts:117-158 | low | correctness | mintDemoUsdcToWallet awaits wallet.sendBatch (returns receipt with boolean success; reverted UserOp resolves success:false not throw) but extracts only hashes and returns `{ success: true }` unconditionally, so a reverted demo-USDC mint reports HTTP 200 success with a real userOpHash; the sibling faucet path checks `if (!result.success)` and returns 500 (backend-boundary twin of F212) | new | none |
| F288 | 13 | backend | packages/demo/backend/src/controllers/wallet.ts:85-105,111-133 | low | info | getLendPosition/getBorrowPosition run validateRequest (and borrowService.resolveBorrowMarketId, which 403s on non-allowlisted markets) BEFORE requireAuth, so an unauthenticated caller distinguishes allowlisted (401-after-resolve) vs non-allowlisted (403) markets — an allowlist-membership oracle; mutating siblings are auth-first | new | none |
| F289 | 13 | backend | packages/demo/backend/src/controllers/wallet.ts:99-102,123-126 | low | correctness | getLendPosition `throw new Error('Wallet not found')` (no try/catch) falls through mapSdkError to a generic 500, while adjacent getBorrowPosition and getWallet return errorResponse(...,404) for the identical missing-wallet condition; mislabels a client-state 404 as a server 500 | new | none |
| F290 | 13 | backend | packages/demo/backend/src/helpers/schemas.ts:46 | low | correctness | Human-amount validators (AmountByHuman z.number().positive(), plus lend/swap inline z.number().positive()) reject NaN but NOT Infinity (Infinity>0 true), so a non-finite amount passes the request boundary and reaches SDK parseUnits/parseAssetAmount as an opaque 500 instead of a clean 400; the AmountByRaw .max(78) branch shows the intended discipline (relates F151/F041) | new | #303 |
| F291 | 13 | backend | packages/demo/backend/src/config/actions.ts:86-91 | low | info | getPrivyClient() returns `new PrivyClient(...)` with no memoization and is called per authenticated request in authMiddleware (auth.ts:24) and again in getWallet (wallet.ts:36) plus once at init, so each mutation constructs >=2 throwaway Privy clients on the hot auth path; perf/throughput, compounds F276 (reported by two surfaces this pass, dup consolidated) | new | none |
| F292 | 13 | backend | packages/demo/backend/src/services/mirror.ts:41-84 | low | info | Aave borrow/repay USDC_DEMO mirror is `void mintMirrorUsdc/removeMirrorUsdc` fire-and-forget: a second sponsored sendBatch in a try/catch that swallows failures and never reads the UserOp receipt success flag, with no idempotency key, so a client retry double-mirrors (no rate limit, F276) and a permanently-failing mirror drifts the demo balance; permissionless mock token, no protocol fund loss | new | none |
| F293 | 13 | backend | packages/demo/backend/src/middleware/actions.ts:5-12 | low | info | actionsMiddleware wraps `getActions(); await next()` in one try with an unbound empty `catch {}` that returns 'Actions SDK not initialized' 500 for ANY error, so every downstream error thrown before a handler's own try/catch is mislabeled and never reaches the global onError structured logger (no stderr log) | new | none |
| F294 | 13 | backend | packages/demo/backend/src/middleware/auth.ts:38-40 | low | correctness | parseAuthorizationHeader uses `value.replace('Bearer','').trim()` — unanchored, first-occurrence string replace — instead of a prefix strip (`value.slice('Bearer '.length)` / `/^Bearer\s+/`); benign for JWT access tokens today but does not match its only-strip-the-scheme intent for tokens containing the substring | new | none |
| F295 | 13 | backend | packages/demo/backend/src/config/env.ts:18-56 | low | correctness | getFaucetAddressDefault re-parses LOCAL_DEV via envalid's private `bool()._parse(...)` during cleanEnv default eval (duplicates validation, couples to an internal API) and, when LOCAL_DEV=true, reads latest-faucet-deployment.json and on ANY read/JSON failure console.warns and silently returns a hardcoded faucet address, so a stale/missing file drips against the wrong contract | new | none |
| F296 | 13 | backend | packages/demo/backend/src/config/env.ts:50-67 | low | info | Signing-critical secrets/addresses (SESSION_SIGNER_PK, FAUCET_*_PRIVATE_KEY, OP_SEPOLIA_FAUCET_ADDRESS, AUTH_MODULE_ADDRESS) are typed bare str() with no 0x-hex/64-char/isAddress validation, unlike the carefully-validated FAUCET_ADDRESS; a malformed key/address passes startup and fails deep inside viem signing as an opaque 500 instead of at boot (fail-fast/sibling-validation gap, relates F273) | new | none |
| F297 | 13 | backend | packages/demo/backend/src/helpers/schemas.ts:47-49,57-61,67-75 | low | correctness | AmountByRaw is `z.string().regex(/^\d+$/).max(78)` with NO positivity refine while sibling AmountByHuman requires .positive(), so amountRaw:"0" (BigInt 0n) is accepted into every fund-moving borrow route; the SDK does not close the raw path (validateAmountPositiveIfExists checks only number amount, Morpho drops 0n), giving a zero-borrow open (Aave revert / Morpho silent-drop) (relates F015) | new | #303 |
| F298 | 13 | backend | packages/demo/backend/src/helpers/validation.ts:66-76 | low | info | validateRequest populates params/query/body only when the top-level schema exposes `.shape`; for any non-ZodObject schema (discriminatedUnion/.refine()/.transform()/union) schemaShape falls back to {} and safeParse({}) runs against an empty object — fails-closed with all current z.object callers, latent footgun where a future union-schema caller validates against zero request data | new | none |
| F299 | 13 | backend | packages/demo/backend/src/helpers/schemas.ts:28-42 | low | info | ChainIdSchema/ChainIdStringSchema validate only positive-integer and cast `as SupportedChainId` without membership in SUPPORTED_CHAIN_IDS, unlike swap.ts chainIdFromNumber/chainIdFromString; mitigated for borrow by allowlist re-resolution (m.chainId===chainId), but a future shared-schema consumer without that re-check forwards an unsupported chainId into SDK chain lookups (shared-schema locus of F284) | new | #334 |
| F300 | 13 | backend | packages/demo/backend/src/helpers/errors.ts:45-56,89-168 | low | info | SDK_ERROR_MAPPINGS comment asserts 'classes never overlap so order is insignificant' — unverified invariant; a future SDK error subclass of an already-mapped class would be shadowed by the base-class mapping (wrong status) under array order; errors.ts is the canonical opaque-literal-message contract that assets.ts (F282) violates | new | none |
| F301 | 13 | backend | packages/demo/backend/src/utils/explorers.ts:19-53 | low | info | getTransactionUrl/getUserOperationUrl interpolate raw transactionHash/userOpHash into `${baseUrl}/tx/${hash}` returned to the client with no /^0x[0-9a-fA-F]{64}$/ shape check; hashes are server-sourced (no injection, display-only) but a malformed/empty hash from a future receipt shape yields a broken URL silently (realistic source: extractReceiptHashes [undefined], F133/F221) | new | none |
| F272 | 12 | backend | packages/demo/backend/src/router.ts:57 | high | fund-loss | POST /wallet/eth faucet drip is the only /wallet/* mutation route registered WITHOUT authMiddleware; handler reads walletAddress from the body and signs an admin drip via FAUCET_AUTH_MODULE_ADMIN_PRIVATE_KEY, gated only by a trivially-bypassable on-chain balance==0 check (TODO second guard unimplemented), so an unauthenticated caller drains faucet ETH and bundler sponsorship across unlimited fresh addresses; sibling /wallet/usdc is auth-gated and mints only to the caller's own wallet | new | none |
| F273 | 12 | backend | packages/demo/backend/src/config/env.ts:50-53 | medium | malicious-sign | FAUCET_ADMIN_PRIVATE_KEY hard-defaults (via `default`, applies in production, unlike sibling devDefault secrets) to the well-known Anvil account-0 key 0xac0974... and is committed in .env.example; currently dead/unused (active signer is FAUCET_AUTH_MODULE_ADMIN_PRIVATE_KEY) so a latent foot-gun, but a known-compromised key in the admin-key slot that a prod deploy resolves silently when unset | new | none |
| F274 | 12 | backend | packages/demo/backend/src/middleware/auth.ts:9-36 | medium | correctness | authMiddleware verifies the Bearer access token via verifyAuthToken but selects the signing wallet from a separate, UNVERIFIED privy-id-token header (getWallet -> users().get({id_token})); the two tokens are never bound to the same Privy user, so a valid access token presented with a victim's id token transacts against the victim's wallet (linchpin: whether Privy's get({id_token}) itself verifies the token) | new | none |
| F275 | 12 | backend | packages/demo/backend/src/services/faucet.ts:31-46,48-95 | medium | fund-loss | Faucet eligibility is a racy balance==0 read with no per-recipient accounting: (a) no atomicity between read and drip so N concurrent requests for one fresh address all drip (TOCTOU), and (b) a recipient swept back to 0 (or fresh addresses) re-qualifies indefinitely; intended position-check second guard is an unimplemented TODO (faucet.ts:42), so even with auth restored a single user can multiply drips | new | none |
| F276 | 12 | backend | packages/demo/backend/src/router.ts:44-104 | medium | infra | No rate-limiting or request-body-size middleware anywhere (createApp/router); fund-touching endpoints /wallet/eth, /wallet/usdc (mints 100 USDC_DEMO/call), /swap/execute, /borrow/position/*, /lend/position/* can be hammered as fast as the bundler accepts, compounding the faucet drain (F272/F275) and burning session-signer + sponsorship budget; AmountByRaw.max(78) caps one amount string but nothing caps rate/volume | new | none |
| F277 | 12 | backend | packages/demo/backend/src/app.ts:96-117 | low | info | CORS origin resolver calls origin.startsWith/origin.match (app.ts:98,109) before any null-check; Hono passes undefined for requests with no Origin header (older versions), throwing a TypeError surfaced as a 500 from the cors layer for any Origin-less request; availability/robustness, does not weaken the allowlist | new | none |
| F278 | 12 | backend | packages/demo/backend/src/app.ts:96-117 | low | info | When LOCAL_DEV is true the CORS resolver reflects ANY http://localhost:* origin (arbitrary ports) with Authorization/privy-id-token allowed; a single stray LOCAL_DEV=true on a hosted/preview deploy lets any page firing an XHR at localhost ride the user's tokens against the API; prod origins are a tight allowlist (good); exploitation requires the deploy-time misconfig | new | none |
| F279 | 12 | backend | packages/demo/backend/src/services/wallet.ts:117-129 | low | info | Demo USDC mint /wallet/usdc is auth-gated and server-fixed to 100 USDC bound to the caller's own wallet (no fund-loss), but (1) the amount is built via BigInt(Math.floor(parseFloat('100')*1e6)) — the float-truncation foot-gun pattern, harmless on the exact literal but unsafe if made request-driven — and (2) has no per-user rate limit on a sponsored UserOp, so a user loops it to burn sponsorship; calls raw SDK sendBatch (review-pass-03 surface) | new | none |
| F280 | 12 | backend | packages/demo/backend/src/services/swap.ts:95-102 | low | info | swap controller validates shape (slippage clamped [0,0.5]) then calls wallet.swap.execute with raw caller params; /swap/execute re-derives a fresh quote inside execute with no binding to the /swap/quote price and no server-side check the executed pair/provider matches what was quoted, inheriting the SDK getQuote-then-execute / per-provider-blocklist gaps (F262) and the slippage>=1.0 / recipient-in-bytes paths (F186); backend adds no compensating control (review-only) | new | #334 |
| F281 | 12 | backend | packages/demo/backend/src/controllers/lend.ts:61-118 | low | info | lend open/close accept tokenAddress and marketId.{address,chainId} independently from the body and forward both to wallet.lend.openPosition with no server-side assertion that the resolved asset is the market's underlying; unlike the borrow controller (re-resolves kind/market from an allowlist), the lend controller has no asset/market reconciliation, so it relies entirely on SDK F008 (open does not validate caller asset vs market underlying -> max-mode approval on a mismatched token); review-only | new | #334 |
| F282 | 12 | backend | packages/demo/backend/src/controllers/assets.ts:16-23 | low | info | getAssets returns raw error.message in its 500 body, breaking the opaque-error contract every sibling handler follows via errorResponse (helpers/errors.ts:45-56, which logs detail to stderr only so internal addresses/RPC URLs/stack fragments don't leak); read-only endpoint, no funds, but an information-disclosure inconsistency in an otherwise carefully opaque error surface | new | none |
| F262 | 11 | swap | packages/sdk/src/actions/swap/namespaces/BaseSwapNamespace.ts:131-142 | medium | correctness | getMarket iterates ALL providers in a try/catch{continue} loop and returns the first success, swallowing MarketNotAllowedError; a pair blocklisted on one provider (e.g. Uniswap USDC/WETH) is silently re-admitted via a sibling provider (Velodrome) with a looser policy, so the per-provider blocklist is not config-wide and an operator's block intent is defeated (sharper fund-safety consequence than F098's opaque-logging) | new | #334 |
| F263 | 11 | swap | packages/sdk/src/actions/swap/namespaces/BaseSwapNamespace.ts:220-242 | medium | correctness | Price-routing best-quote comparator (getBestQuote 225-230, getQuotes sort 104-110) maximizes gross quote.amountOutRaw and never reads quote.gasEstimate, which both providers populate; for small/multi-hop swaps the 'best' route can be net-worse after gas yet is pre-built and signed, contradicting documented routing:'price' (distinct from F114 exact-output mis-objective) | new | #435 |
| F264 | 11 | swap | packages/sdk/src/actions/swap/namespaces/WalletSwapNamespace.ts:75-82 | low | malicious-sign | Pre-built quote execute re-resolves the provider from quote.provider with no assertion that quote.provider matches quote.execution.routerAddress; a quote with provider='uniswap' but execution.routerAddress=velodrome router builds the Permit2/token approval for the Uniswap router while the swap targets the velodrome router, so the user signs an approval to a router they never intended (defense-in-depth hole on the F070 verbatim path, distinct: provider<->routerAddress consistency not router-allowlist) | new | #373 |
| F265 | 11 | borrow | packages/sdk/src/actions/borrow/namespaces/BaseBorrowNamespace.ts:111-134 | low | info | getProviderForMarket kind-fallback can route a market to a provider whose allowlist does NOT contain it; harmless today because every entry point re-asserts the allowlist (requireAllowlistedMarketConfig) but the two-tier routing reads as 'kind alone authorizes a market' and is a latent footgun if a future provider hook forgets the re-check (relates F016) | new | #334 |
| F266 | 11 | wallet-core | packages/sdk/src/wallet/core/namespace/WalletNamespace.ts:228-239 | low | infra | resolveProvider sets _initPromise once and never clears it on rejection, so one transient factory failure permanently poisons every later createSmartWallet/getSmartWallet/createSigner/hostedWalletProvider/smartWalletProvider call for the namespace lifetime; sibling Wallet.initialize clears initPromise on failure to allow retry, so two init paths in the same module disagree on retryability | new | none |
| F267 | 11 | wallet-core | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:196-207,562-587 | medium | correctness | Counterfactual smart wallet derives its reported address two independent ways never asserted equal: this._address from the SDK factory getAddress(signerBytes,nonce) (what integrators fund) vs getCoinbaseSmartAccount passing address:deploymentAddress(undefined) so viem re-derives the UserOp sender internally from owners; if SDK formatPublicKey owner-bytes and viem's owner encoding ever diverge, funds land at one address while a different contract operates on-chain (passing this._address into getCoinbaseSmartAccount collapses to one source; extends F237) | new | none |
| F268 | 11 | wallet-hosted | packages/sdk/src/wallet/node/providers/hosted/privy/PrivyHostedWalletProvider.ts:60-95 | low | correctness | Same provider, two address-handling contracts: toActionsWallet (67) passes getAddress(params.address) but createSigner (90-94) spreads {...params} so params.address reaches createViemAccount unnormalized; behavior depends on which public method the integrator called, with no shared normalization helper (divergent-contract framing of F028) | new | none |
| F269 | 11 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:224-236,268-281 | low | info | send/sendBatch call prepareUserOperation then forward only callData/initCode into sendUserOperation, which (account present) re-runs the FULL prepare incl. paymaster:true, so every sponsored send fires pm_sponsorUserOperation TWICE and discards the first; sponsorship-policy counters/rate-limits consumed at 2x and per-quote paymaster charges burned (sharpens F037's generic wasted-prepare with the double-sponsorship angle) | new | #456 |
| F270 | 11 | core-services | packages/sdk/src/services/nameservices/ens/EnsNamespace.ts:52-60,78-113,126-183 | low | infra | addressCache/nameCache/infoCache insert one entry per call with an expiresAt but TTL is only checked on read and expired entries are never deleted (no delete/clear/LRU/size cap); a long-lived Actions instance resolving attacker-influenceable ENS names/addresses accumulates one permanent map entry per distinct input, an unbounded per-process memory-exhaustion/DoS vector distinct from the ENS resolution-correctness findings (F068/F096/F113) | new | #453 |
| F271 | 11 | core-services | packages/sdk/src/services/ChainManager.ts:140-155,109-115 | low | correctness | getBundlerUrl JSDoc claims it returns 'a string or undefined if not configured' but the body throws ChainNotSupportedError on missing bundler and otherwise returns a non-empty url, so it can never return undefined; the downstream `if (!bundlerUrl) throw` guard in getBundlerClient (109-115) is unreachable dead code and a future refactor trusting the JSDoc would mis-handle the throwing contract | new | none |
| (refines:F004) | 11 | swap | packages/sdk/src/actions/swap/core/SwapProvider.ts:270-277,453-471 | low | correctness | resolveQuoteDefaults computes amountInRaw = parseAssetAmount(assetIn, amountIn ?? 1), so an exact-output quote (amountIn undefined) fabricates a 1-unit placeholder that flows unchecked into execution.value (native-in) and amountInRaw display fields for EVERY exact-output quote; the `?? 1` default is the shared whole-flow root of the native-in exact-out value=placeholder revert (the exploitable native instance is refines:F004 at UniswapSwapProvider:172) | refines:F004 | none |
| (refines:F008) | 11 | lend | packages/sdk/src/actions/lend/core/LendProvider.ts:209-218 | low | correctness | closePosition unconditionally throws AssetMetadataRequiredError when params.asset?.metadata is undefined (lines 209-212), i.e. the documented omitted-asset/marketId-driven case, making the `params.asset ?? market.asset` fallback on line 216 dead code; the throw is fail-closed (not fund-loss) but it forbids the safer close-by-marketId shape and forces every integrator to pass a caller asset whose decimals are trusted over the market's own (the F008 decimals hazard) | refines:F008 | #334 |
| (refines:F011) | 11 | lend | packages/sdk/src/actions/lend/providers/aave/AaveLendProvider.ts:70-83 | low | correctness | _openPosition calls getMarket solely to populate the informational apy snapshot (Aave -> getReserve -> fresh ethers JsonRpcProvider on the chain's default/public RPC + UiPoolDataProvider, F014/F156); any failure of that heavy read is caught by the broad try/catch and rethrown as a generic 'Failed to open position', so a purely cosmetic APY number gatekeeps signable deposit calldata that needs no reserve data (Morpho twin at MorphoLendProvider:59-62,79-83) | refines:F011 | #334 |
| (refines:F055) | 11 | borrow | packages/sdk/src/actions/borrow/core/quote.ts:54 | medium | correctness | Whole-flow generalization: projected post-action health/LTV is computed independently by both providers (Aave projectAavePositionState healthFactorWad presentation.ts:126-150; Morpho computeOpen/computeClose AccrualPosition) AND surfaced as safeCeilingLtv by the assembler, yet NO point in openPosition->_openPosition->finalizePlan->assembleQuote->dispatch compares projected LTV/health against safeCeilingLtv (or 1.0); a borrow below the SDK's own advertised safe ceiling but above on-chain LLTV is quoted and dispatched with zero solvency gate | refines:F055 | none |
| (refines:F117) | 11 | borrow | packages/sdk/src/actions/borrow/providers/morpho/presentation.ts:40,84 | low | correctness | Sharpens/partially refutes F117: Morpho maxLtv is morphoWadToNumber(marketParams.lltv) and lltv IS the single liquidation LTV (Morpho Blue has no separate borrow vs liquidation LTV), so both providers surface the liquidation threshold as maxLtv and the 'Aave disagrees with Morpho' framing overstates the divergence; F117's real core (Aave maxLtv should use the borrow-LTV bits 0-15, not the discarded liquidation-threshold bits) stands, the cross-provider disagreement claim should be dropped | refines:F117 | none |
| (refines:F054) | 11 | borrow | packages/sdk/src/actions/borrow/namespaces/WalletBorrowNamespace.ts:218 | low | correctness | validateQuoteNotExpired (validation.ts:87-92) only checks now>=quote.expiresAt; expiresAt is a plain caller-supplied field not bound/signed against the calldata, with no quotedAt<=now / expiresAt>quotedAt / window<=quoteExpirationSeconds sanity, so a relayed/cached quote can self-extend its validity indefinitely (recipient==wallet guard prevents fund-redirect, impact compounds only with the F054 calldata-binding gap); the expiry slice of F054 | refines:F054 | none |
| (refines:F015) | 11 | borrow | packages/sdk/src/actions/borrow/providers/morpho/close.ts:36-42 | low | correctness | Morpho 'max' close resolves a {max:true} collateral leg to after.collateral (a fixed quote-time bigint snapshot) and encodeMorphoWithdrawCollateral sends it verbatim, with no Morpho equivalent of Aave's maxUint256 live sentinel (write.ts:115); if collateral moves between quote and execution (e.g. partial liquidation) the Morpho max-close reverts on the over-withdraw while the Aave max-close clears to live balance, a cross-provider max-semantics asymmetry under staleness (distinct from the missing >0 guard) | refines:F015 | none |
| (refines:F015) | 11 | borrow | packages/sdk/src/actions/borrow/providers/aave/quote.ts:109-111 | low | correctness | buildAaveOpenQuoteArgs unconditionally appends encodeAaveBorrow(market, borrowAmountWei, user) even when borrowAmountWei=0, so a collateral-only open (borrowAmount {amountRaw:0n}) emits a guaranteed-revert Pool.borrow(asset,0,2,0,user); Morpho open has the same shape (open.ts:58-66); the open-path face of the missing positivity guard (F015), nothing routes a zero-borrow open to depositCollateral or rejects it | refines:F015 | #303 |
| (refines:F059) | 11 | wallet-core | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:223-250,265-294 | low | info | send/sendBatch call prepareUserOperation then discard every prepared field except callData/initCode when calling sendUserOperation (drops nonce/gas/fees), so viem re-runs the full prepare internally over the suffixed callData; the explicit prepare is wasted work, its gas/fees are not the ones used, paymaster context is recomputed, and the suffix-append is the only reason callData is passed explicitly (this is also the structural reason the v0.7 initCode suffix is a no-op, F059) | refines:F059 | none |
| (refines:F021) | 11 | wallet-core | packages/sdk/src/actions/lend/namespaces/WalletLendNamespace.ts:89-98 | medium | fund-loss | Integration-level reach of the EOA mid-batch-revert hazard: dispatch hands txs=[approval, position] to executeTransactionBatch, which is one atomic UserOp for a smart wallet but sequential-with-no-receipt.status-check for an EOA (EOAWallet:90-100, F021), so an EOA approval lands while the position reverts leaving a standing allowance; the same code path gives two safety guarantees purely by wallet type, live in production lend/swap/borrow whenever the wallet is an EOA/LocalWallet (root EOA bug owned by F021) | refines:F021 | #335 |
| (refines:F212) | 11 | wallet-core | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:261-294,232-247 | medium | correctness | Integration reach of F212 across the namespace boundary: send/sendBatch never check receipt.success (only deploy/addSigner/removeSigner do), and WalletBorrowNamespace.dispatch (232-247) unconditionally builds a BorrowReceipt via extractReceiptHashes(receipt), so a confirmed-but-reverted ERC-4337 op surfaces its userOpHash + quote-derived positionAfter as a completed borrow; lend and swap likewise return the raw union with no success gate, leaking the silent revert into all three action-level success envelopes | refines:F212 | none |
| (refines:F074) | 11 | wallet-hosted | packages/sdk/src/wallet/node/wallets/hosted/turnkey/utils/createSigner.ts:25-31 | medium | malicious-sign | The hosted createSigner public path exports an address-untrusted LocalAccount (Turnkey ethereumAddress/signWith forwarded raw F031; node Privy caller-address no reconcile F028) straight into DefaultSmartWallet's owner logic, which matches owners by exact .address (findSignerInArray) and computes the slot via findSignerIndexOnChain; a non-canonical/mismatched hosted .address surfaces later as a wrong-slot ownerIndex (UserOp revert) or an unsignable owner slot with no construction-time error, linking the address-trust roots F028/F031/F074 to the index logic F090/F107 across modules | refines:F074 | #163 |
| (refines:F244) | 11 | wallet-hosted | packages/sdk/src/wallet/react/wallets/hosted/dynamic/utils/createSigner.ts:24-37 | low | correctness | Dynamic createSigner wires sign->connector.signRawMessage but signMessage/signTransaction/signTypedData->walletClient.*, so EOAWallet.send (signTransaction) never exercises the sign() closure; sign() is only reached when this LocalAccount is a smart-wallet OWNER (4337 account.sign({hash})), so any divergence between the connector key and the walletClient account (or an EIP-191 prefix on the 0x-stripped hash) is invisible in EOA flows and only breaks the smart-wallet path with an invalid owner signature (cross-flow elaboration of F062/F030/F244) | refines:F244 | #163 |
| (refines:F033) | 11 | wallet-hosted | packages/sdk/src/wallet/react/providers/registry/ReactHostedWalletProviderRegistry.ts:22-71 | low | info | All three React validateOptions return true and ReactOptionsMap entries are undefined, so registry construction validates nothing; for Turnkey the signing client + organizationId/signWith/ethereumAddress arrive per-call via toActionsWallet and reach createAccount with zero runtime checks anywhere, and unlike node there is no o?.client truthiness guard, so the React signing path has no single point asserting the client is present/usable before a signature is requested | refines:F033 | none |
| (refines:F137) | 11 | wallet-hosted | packages/sdk/src/wallet/react/providers/hosted/types/index.ts:60-65 | low | correctness | Sharpens F137: node NodeOptionsMap['turnkey']={client} pins the client at provider construction (validated once by the registry) but React TurnkeyHostedWalletToActionsWalletOptions={client,organizationId,signWith,ethereumAddress} supplies the client fresh per toActionsWallet/createSigner call, never pinned or validated, so two wallets from the same provider can sign with two different Turnkey clients; a lifecycle/trust divergence (not only a type-shape divergence) with no shared contract test | refines:F137 | #330 |
| (refines:F059) | 11 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:230-234,276-279 | low | info | F059 premise refuted: viem toCoinbaseSmartAccount hardcodes entryPoint v0.6 regardless of account version '1.1', so prepareUserOperation populates initCode=concat([factory,factoryData]) on the undeployed path and appendAttributionSuffix(uo.initCode) concatenates 16 bytes onto the live factory createAccount(owners,nonce) the EntryPoint executes and the signature covers; deployment-op attribution is NOT silently dropped, confirming F063 is live; F059 conflated account version (1.1) with EntryPoint version (0.6) | refines:F059 | none |
| (refines:F043) | 11 | core-services | packages/sdk/src/services/nameservices/ens/utils.ts:43-47 | low | malicious-sign | resolveAddress returns a hex input verbatim when isAddress(input,{strict:false}) (no EIP-55 checksum, F043) AND with no zero-address guard, whereas the ENS-name branch throws EnsResolutionError on the zero address (78-83); so actions.ens.getAddress('0x00..00') returns 0x0 while an ENS name resolving to it throws, and since resolveAddress is exported as a public utility (index.ts:89) an integrator running their own client without validateRecipient gets a recipient=0x0 path into their signed calldata | refines:F043 | #371 |
| F257 | 10 | swap | packages/sdk/src/actions/swap/providers/uniswap/UniswapSwapProvider.ts:101; encoding.ts:271-273,288 | medium | correctness | Exact-output Uniswap ERC20/Permit2 approval keyed to quote.amountInRaw (un-slipped) not the maxAmountIn the SETTLE_ALL action is authorized to spend; in default 'exact' mode any adverse-price move within the slippage band makes the allowance insufficient and the settle transferFrom reverts, so input-side slippage protection is structurally unusable (distinct from refines:F004 native-value and F048/F182 amountInMaximum-surfacing) | new | none |
| F258 | 10 | swap | packages/sdk/src/actions/swap/providers/velodrome/encoding/routers/cl.ts:135-164; v2.ts:214-237; helpers.ts:30-33 | medium | correctness | Velodrome universal/CL native-ETH OUTPUT silently delivers WETH (no trailing UNWRAP_WETH command), unlike the v2/leaf swapExactTokensForETH path that unwraps; quote.amountOut reported in ETH/WETH (both 18 dec) masks the asset substitution; output-side mirror of F047/refines:F047 (native-IN only) | new | none |
| F259 | 10 | borrow | packages/sdk/src/utils/validateAddresses.ts:121-153 | medium | correctness | validateConfigAddresses (the single config-time address-format choke point, called in actions.ts:86) walks only config.lend/swap/assets and silently skips the entire config.borrow surface (its param type lacks a borrow field), leaving Morpho marketParams.loanToken/collateralToken/oracle/irm and Aave aave.debtReserve/collateralReserve (all signed-calldata targets/approval tokens) never isAddress-validated; verifyMorphoMarketId is no tripwire when marketId is derived from the same typo'd params (distinct from F099/F103/F104) | new | none |
| F260 | 10 | borrow | packages/sdk/src/actions/borrow/providers/morpho/MorphoBorrowProvider.ts:79-88 | medium | correctness | Morpho borrow only runs verifyMorphoMarketId (marketId==keccak(marketParams)); it never binds marketParams.collateralToken/loanToken to the configured collateralAsset/borrowAsset (whose metadata.decimals scale every signed amount) or to the market chainId, so a config pairing an 18-dec asset with a 6-dec (or other-chain) marketParams token over-approves/over-supplies by 10^Δdecimals; asymmetric with the Aave reserve<->asset bind (F103) and distinct from F017 (marketId hash integrity) | new | none |
| F261 | 10 | borrow | packages/sdk/src/actions/borrow/namespaces/WalletBorrowNamespace.ts:220; core/validations.ts:83-102 | medium | malicious-sign | Pre-built borrow quote dispatch gates marketId only via validateBorrowMarketIdInAnyAllowlist (allowlist-only), silently bypassing the marketBlocklist that the re-quote path (requireAllowlistedBorrowMarketConfig) enforces; a developer-disabled (blocklisted) but still-allowlisted market is fully dispatchable via a cached/relayed quote, so the wallet signs borrow/withdraw calldata against a market they explicitly blocked (borrow analog of lend F010, on the pre-built-quote vector; re-quote path DOES enforce it) | new | #334 |
| F177 | 9 | swap | packages/sdk/src/actions/swap/providers/velodrome/__tests__/VelodromeSwapProvider.network.test.ts:262-291 | high | correctness | Only on-chain 'execute' fork test asserts the encoder against itself (tx.data === quote.swapCalldata) and never broadcasts; no swap test proves a swap executes or output lands at the recipient (F046/F003/F047 un-falsifiable) | verified | #335 |
| F178 | 9 | swap | packages/sdk/src/actions/swap/providers/velodrome/__tests__/VelodromeSwapProvider.network.test.ts:107-260 | medium | correctness | Fork tests run only on OP/Base ('v2', recipient honored); the two buggy router paths (universal recipient-ignored sentinel + no native branch, V4) have ZERO on-chain coverage [P10 refined high->medium: test-coverage gap, fund-loss owned by F003/F046/F047] | refines:self | #444 |
| F179 | 9 | swap | packages/sdk/src/actions/swap/providers/velodrome/__tests__/VelodromeSwapProvider.network.test.ts:131-167,224-259 | medium | correctness | Fork quote assertions directional (amountOut/price/min > 0) with no exact-amount oracle recomputed independently of quote.amountOut; a wrong-pool/decimals/stale-price quote passes | new | #318 |
| F180 | 9 | swap | packages/sdk/src/actions/swap/providers/uniswap/__tests__/sdk.test.ts:261-431 | high | fund-loss | encodeUniversalRouterSwap (V4) tests assert only calldata prefix/length/action-code bytes, never decode recipient/TAKE_ALL params; the F046 recipient-ignored fund-loss bug is invisible | verified | #444 |
| F181 | 9 | swap | packages/sdk/src/actions/swap/providers/velodrome/__tests__/encoding.v2.test.ts:177-231 | low | fund-loss | Universal-router encoder test never asserts the encoded recipient while sibling v2/leaf tests do; the asymmetric omission is why the F003/#444 recipient-sentinel bug ships green [P10 refined high->low: test-gap on F003 (medium/correctness, funds go to wallet not attacker)] | refines:self | #444 |
| F182 | 9 | swap | packages/sdk/src/actions/swap/providers/uniswap/__tests__/sdk.test.ts:274-310,367-397 | medium | correctness | Exact-output amountInMaximum (spend ceiling) never decoded/asserted; exact-out tests check only 0x-prefix and exactIn!=exactOut, so a broken maxAmountIn or the F004 placeholder-value bug passes | new | #318 |
| F183 | 9 | swap | packages/sdk/src/actions/swap/namespaces/__tests__/WalletSwapNamespace.spec.ts:236-327 | medium | malicious-sign | requireQuoteForThisWallet tests assert only quote.recipient METADATA, never the recipient encoded inside execution.swapCalldata; the guard's known no-op on sentinel/msg.sender paths is untested | new | #437 |
| F184 | 9 | swap | packages/sdk/src/actions/swap/namespaces/__tests__/BaseSwapNamespace.spec.ts:58-232 | medium | correctness | Best-quote routing tested only for exact-IN (maximize amountOut); no test covers the F114 exact-OUT mis-objective where the comparator should minimize amountIn cost | new | none |
| F185 | 9 | swap | packages/sdk/src/actions/swap/providers/velodrome/__tests__/VelodromeSwapProvider.network.test.ts:92-100 | low | infra | Anvil fork harness uses hardcoded fixed ports (18545/18546); parallel runs or a leftover process collide and the suite fails or silently forks the wrong upstream | new | #332 |
| F186 | 9 | swap | packages/sdk/src/actions/swap/core/SwapProvider.ts:286-298,440-451 | medium | fund-loss | No test exercises slippage>=1.0/NaN/negative-min-out on computeSlippageBounds via getQuote (validateSwapExecute skipped there); the negative-amountOutMinRaw fund-loss path is uncovered | new | #373 |
| F187 | 9 | swap | packages/sdk/src/actions/swap/core/SwapProvider.ts:333-392 | medium | malicious-sign | buildPermit2Approvals max-mode/expiration has no test asserting the residual allowance after a swap (maxUint160 / 30-day standing spend) nor the permit2-expiry vs swap-deadline decoupling | new | #436 |
| F188 | 9 | swap | packages/sdk/src/actions/swap/providers/velodrome/__tests__/VelodromeSwapProvider.network.test.ts:1-293 | medium | info | CONSOLIDATED E2E SPEC (swap portion): single Anvil feature-test must execute real swaps on every router/direction with exact-amount recipient-balance assertions and recipient/allowance/quote-aging adversarial cases [P10 refined high->medium: e2e-spec info item, fund-loss owned by F046/F047/F004] | refines:self | #335 |
| F189 | 9 | lend | packages/sdk/src/actions/lend/providers/morpho/__tests__/MorphoLendProvider.test.ts:16-23,186-213,86-114 | medium | correctness | Morpho deposit/withdraw signing path has ZERO calldata-arg verification: MetaMorphoAction mocked to a literal hex, so receiver/owner/amount in the signed tx are never asserted [P10 refined high/malicious-sign->medium/correctness: production path correct; gap is test-coverage + caret-dep regression risk] | refines:self | #335 |
| F190 | 9 | lend | packages/sdk/src/actions/lend/providers/aave/__tests__/AaveLendProvider.test.ts:125-205,213-271,355-375 | high | malicious-sign | Aave supply/withdraw/depositETH/withdrawETH calldata never decoded in any test; onBehalfOf/to/pool/amount args (incl. native-ETH recipient-in-bytes) in the signed tx are unverified | verified | #335 |
| F191 | 9 | lend | packages/sdk/src/actions/lend/providers/aave/__tests__/AaveLendProvider.test.ts:1-20,56-62 | medium | info | Entire lend surface is mocked: NO fork/network test exists for either Aave or Morpho lend (open or close), so no real on-chain behavior is ever exercised [P10 refined high->medium: info-class umbrella, concrete risks owned by medium/low siblings F192/F193/F196/F197] | refines:self | #332 |
| F192 | 9 | lend | packages/sdk/src/actions/shared/aave/__tests__/pool.test.ts:11-31 | medium | correctness | Aave pool ABI round-trip encodes and decodes with the SAME local ABI (encoder-vs-itself); no independent oracle that POOL_ABI matches the real Aave Pool signatures; supply/withdraw not round-tripped | new | #318 |
| F193 | 9 | lend | packages/sdk/src/actions/lend/providers/aave/__tests__/AaveLendProvider.test.ts:150-151; morpho/__tests__/MorphoLendProvider.test.ts:113 | medium | correctness | Directional non-assertions on APY (apy > 0) and balance formatting masquerade as coverage; apy proves the fixture not calculateApyBreakdown; Aave getPosition balance has no exact assertion | new | #209 |
| F194 | 9 | lend | packages/sdk/src/utils/test.ts:74-105; borrow/.../MorphoBorrowProvider.network.test.ts:161 | medium | infra | startAnvilFork uses caller-supplied FIXED ports (18545/46/47) with no collision avoidance; a new lend fork test on a hand-picked port races the existing network suites under parallel vitest | new | #332 |
| F195 | 9 | lend | packages/sdk/src/utils/test.ts:324-392 | medium | infra | fundWallet's USDC whale-impersonation swallows transfer failures (catch -> console.log) and hard-codes a Unichain whale/USDC; a lend fork test proceeds with zero balance and a misleading directional pass | new | #332 |
| F196 | 9 | lend | packages/sdk/src/actions/lend/providers/morpho/__tests__/MorphoLendProvider.test.ts:57-138 | low | correctness | No deposit->withdraw roundtrip invariant test (open then close) for either provider; open and close tested in isolation against independent mocks; the canonical vault invariant is unexpressed | new | #335 |
| F197 | 9 | lend | packages/sdk/src/actions/lend/providers/aave/sdk.ts:253-264 | low | correctness | getATokenAddress's index-8 tuple destructure of getReserveData has no test pinning the index against the real Aave Pool struct layout (the aToken is the close-approval spender + position contract) | new | #211 |
| F198 | 9 | lend | packages/sdk/src/actions/lend/core/LendProvider.ts:84-118; morpho/MorphoLendProvider.ts:48-84 | low | malicious-sign | No residual-allowance / approvalMode='max' adversarial test: after a max-approval supply nothing verifies the leftover allowance's spender is the correct vault/pool (a wrong spender is a fund-drain primitive) | new | #133 |
| F199 | 9 | lend | packages/sdk/src/actions/lend/providers/morpho/sdk.ts:341-394 | low | correctness | getVault SDK->on-chain fallback never tested in the divergent direction; no test asserts the SDK-path and forced-fallback paths produce the same market shape (two-divergent-harnesses parity) | new | #211 |
| F200 | 9 | lend | docs/reviews/review-pass-09.md (e2e-spec deliverable) | low | info | CONSOLIDATED E2E SPEC (lend slice): single Anvil feature-test must run real-signed Morpho+Aave supply/withdraw on an OP fork with exact-amount decode (independent ABI), roundtrip, APY-parity, recipient-in-bytes/residual-allowance/divergent-harness cases [P10 refined high->low: info-class spec, n/a exploit, teeth in siblings F189-F199; sibling spec F223 is medium] | refines:self | #335 |
| F201 | 9 | lend | packages/sdk/src/actions/lend/providers/aave/__tests__/AaveLendProvider.test.ts:150-151,239-240 | medium | correctness | Aave-locus companion to F193: the Aave getPosition share/balance path (shares===balance 1:1, decimals-formatted) has no exact balance test; combined with F116 the Aave APY/share-price is fixture-bound only | new | #209 |
| F202 | 9 | lend | packages/sdk/src/utils/test.ts:74-105,116-438 | medium | infra | The lend e2e leg compounds both harness blockers (a 4th free port AND working per-chain USDC funding at once) and has no prior fork test to extend; harness consolidation must precede the lend leg | new | #332 |
| F203 | 9 | borrow | packages/sdk/src/actions/borrow/namespaces/WalletBorrowNamespace.ts:232-247 | medium | malicious-sign | Borrow signing/dispatch path has ZERO on-chain coverage; no test signs+broadcasts a borrow bundle (the F054/F082/F084 verbatim-calldata cluster is structurally uncatchable) [P10 refined high->medium: test-coverage of already-correct dispatch; signing vulns owned by F054/F082/F084] | refines:self | #335 |
| F204 | 9 | borrow | packages/sdk/src/actions/borrow/providers/morpho/__tests__/marketParams.spec.ts:20-39 | medium | correctness | Morpho marketId test asserts the encoder against itself (re-derives keccak256(abi.encode) with the same tuple); no golden vector from a deployed Morpho Blue market; fixtures derive marketId FROM the function under test | new | #335 |
| F205 | 9 | borrow | packages/sdk/src/actions/borrow/providers/aave/__tests__/marketId.test.ts:12-27 | low | correctness | computeAaveBorrowMarketId tested only for determinism/uniqueness/self-round-trip, never against a stable golden vector; a hashing-scheme regression (F104) re-namespaces every allowlisted market silently | new | #335 |
| F206 | 9 | borrow | packages/sdk/src/actions/borrow/providers/morpho/__tests__/MorphoBorrowProvider.network.test.ts:181-220 | medium | correctness | Morpho fork test uses non-assertions (toBeGreaterThan(0), zeros-on-a-fresh-wallet, length+functionName only) and never funds/signs/broadcasts; 'open emits a borrow' proven only at ABI-selector level | new | #335 |
| F207 | 9 | borrow | packages/sdk/src/actions/borrow/providers/morpho/__tests__/MorphoBorrowProvider.network.test.ts:161 | low | infra | Hardcoded fixed Anvil port (18547) collides with the Velodrome fork tests (18545/46) under parallel/leaked runs; static hand-chosen constants with no collision guard | new | #332 |
| F208 | 9 | borrow | packages/sdk/src/actions/borrow/providers/morpho/__tests__/MorphoBorrowProvider.network.test.ts:50-102 | low | infra | Borrow fork test silently self-skips (describe.skip) when deployments.json is unpopulated; in CI without the demo deploy the only borrow fork test disappears and the build is green with zero coverage | new | #332 |
| F209 | 9 | borrow | packages/sdk/src/actions/borrow/providers/aave/__tests__/AaveBorrowProvider.write.spec.ts:102-149 | medium | correctness | Aave write tests assert positionAfter against hand-coded mock oracle prices (projection verified against its own inputs); no Aave borrow fork test exists, so the projection + WETHGateway path is mock-only | new | #335 |
| F210 | 9 | borrow | packages/sdk/src/actions/borrow/namespaces/WalletBorrowNamespace.ts:207-247 | medium | malicious-sign | E2E spec: borrow must exercise recipient-in-bytes (onBehalf/receiver=attacker while quote.recipient spoofed), residual maxUint256 allowance, and quote-aging via Anvil time-travel on a real fork [P10 refined high->medium: e2e-test-spec slice; underlying validation gap owned high by F054] | refines:self | #335 |
| F211 | 9 | wallet-core | packages/sdk/src/wallet/core/wallets/eoa/__tests__/EOAWallet.spec.ts:90-321 | low | correctness | EOA sendBatch has ZERO real-execution coverage of the mid-batch-revert residual-allowance hazard (F021); the suite mocks sendTransaction+waitForTransactionReceipt with a hardcoded status:'success' [P10 refined high/fund-loss->low/correctness: test-debt; fund-loss owned by F021] | refines:self | #335 |
| F212 | 9 | wallet-core | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:261-294 | medium | correctness | Smart-wallet send/sendBatch never assert receipt.success (only deploy does); no test covers a success:false UserOp receipt, so a reverted UserOp is reported as a successful action to dispatch [P10 refined high->medium: real silent-revert bug but harm is mislabeled-correctness not fund-loss; systemic w/ EOA F020] | refines:self | #474 |
| F213 | 9 | wallet-core | packages/sdk/src/wallet/core/wallets/smart/default/__tests__/DefaultSmartWallet.spec.ts:181,254-255 | medium | correctness | Attribution-suffix callData test asserts concatHex against concatHex (encoder-vs-itself) with no independent decode of the suffixed UserOp callData (wallet-core analog of F153) | new | #373 |
| F214 | 9 | wallet-core | packages/sdk/src/wallet/core/utils/__tests__/executeTransactionBatch.spec.ts:17-41 | medium | correctness | executeTransactionBatch dispatcher tested only against a stubbed wallet ({send,sendBatch} vi.fn); the single-vs-batch routing boundary and the untagged-union receipt shape divergence (F132) are unverified | new | #337 |
| F215 | 9 | wallet-core | packages/sdk/src/wallet/core/namespace/WalletNamespace.ts:22-34,190-206 | medium | malicious-sign | isLocalAccount routing untested for the hosted-signer collision: a Privy/Turnkey-derived signer is itself type:'local', so passing it routes to LocalWallet and silently bypasses the hosted provider (F074) | new | none |
| F216 | 9 | wallet-core | packages/sdk/src/utils/test.ts:74-105,116-438 | medium | infra | E2E harness blockers: startAnvilFork uses FIXED ports (collide on parallel/CI reruns) and fundWallet hardcodes Unichain-only USDC + a single whale, so any non-Unichain signing e2e silently no-ops USDC funding | new | #332 |
| F217 | 9 | wallet-core | packages/sdk/src/actions/swap/providers/velodrome/__tests__/VelodromeSwapProvider.network.test.ts:108-167 | medium | correctness | The only two network tests are read/quote-only with directional assertions (amountOut > 0) and no independent oracle; ZERO network coverage of any wallet-core signing/dispatch path | new | #335 |
| F218 | 9 | wallet-core | packages/sdk/src/actions/borrow/providers/morpho/__tests__/MorphoBorrowProvider.network.test.ts:104-117 | low | infra | Two divergent inline fork-harness shapes (createForkChainManager differs per file: 1-arg baseSepolia-hardcoded vs 2-arg chain-param); no shared harness wiring a signing client for the e2e to build on | new | #332 |
| F219 | 9 | wallet-core | packages/sdk/src/wallet/core/utils/__tests__/retryOnStaleRead.spec.ts:1-39 | low | correctness | retryOnStaleRead final unconditional re-read path (F135) has no test; an all-attempts-stale-then-final-throw propagates instead of returning the stale value the doc implies | new | none |
| F220 | 9 | wallet-core | packages/sdk/src/wallet/core/wallets/eoa/EOAWallet.ts:33-52 | low | correctness | nonceManager sequencing asserted only structurally (mock checks createWalletClient args), never that back-to-back sends receive sequential nonces (n, n+1) against a real node | new | #456 |
| F221 | 9 | wallet-core | packages/sdk/src/wallet/core/utils/extractReceiptHashes.ts:24-30 | low | correctness | extractReceiptHashes [undefined]/empty-batch degenerate outputs (F133) surfaced to callers with no test; a regression producing transactionHashes:[undefined] (unusable hash) ships green | new | none |
| F222 | 9 | wallet-core | packages/sdk/src/wallet/core/wallets/smart/default/__tests__/DefaultSmartWallet.spec.ts:81-124 | low | correctness | Deterministic CREATE2 address + getCoinbaseSmartAccount mocked (toCoinbaseSmartAccount vi.fn), so the funds-receiving undeployed address is never pinned to a real viem golden vector on the send path (sharpens F171) | new | #131 |
| F223 | 9 | wallet-core | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:261-294 | medium | info | CONSOLIDATED E2E SPEC (wallet-core): single Anvil feature-test must drive real signed send/sendBatch through EOAWallet AND DefaultSmartWallet with exact-amount deltas and recipient-in-bytes/residual-allowance/quote-aging cases | new | #335 |
| F224 | 9 | wallet-core | packages/sdk/src/wallet/core/wallets/smart/default/__tests__/DefaultSmartWallet.spec.ts:254-255 | low | correctness | The sendBatch initCode+suffix assertion (concatHex on both sides) is the second self-referential locus of F213, covering the deployment-op suffix leg (F063/F059) with no independent decode | new | #373 |
| F225 | 9 | wallet-hosted | packages/sdk/src/wallet/react/wallets/hosted/dynamic/utils/__tests__/createSigner.spec.ts:52,60-67 | high | malicious-sign | Dynamic createSigner test mocks toAccount, so the hand-rolled raw-digest sign() closure (hex-stripping + cross-backend signRawMessage, the EIP-712/Permit2 path, F030/F062) is never executed by any test | demoted | #335 |
| F226 | 9 | wallet-hosted | packages/sdk/src/wallet/node/providers/hosted/privy/__tests__/PrivyHostedWalletProvider.spec.ts:120-141 | medium | malicious-sign | Privy createSigner/toActionsWallet address tests assert the mock against itself (empty MockPrivyClient cannot resolve walletId->key); structurally cannot catch the caller-address vs signing-key divergence (F029/F074) | new | #335 |
| F227 | 9 | wallet-hosted | packages/sdk/src/wallet/node/wallets/hosted/turnkey/utils/__tests__/createSigner.spec.ts:53-76 | medium | malicious-sign | Turnkey ethereumAddress shortcut asserted only as forwarded-to-mock; no test that a caller-supplied ethereumAddress is reconciled against the signWith key (F031); the only test confirms the bypass works | new | #335 |
| F228 | 9 | wallet-hosted | packages/sdk/src/wallet/node/providers/hosted/registry/__tests__/NodeHostedWalletProviderRegistry.spec.ts:76-88 | low | malicious-sign | Registry validateOptions tests pin only client-truthiness; the signing-key selectors (organizationId/signWith/ethereumAddress) are never asserted as validated, locking in the F033 no-op-guard contract | new | none |
| F229 | 9 | wallet-hosted | packages/sdk/src/wallet/react/wallets/hosted/privy/utils/__tests__/createSigner.spec.ts:16-20,57-63 | low | malicious-sign | React Privy createSigner mocks toAccount and asserts signTypedData was forwarded, but never recovers a signer from a Permit2/EIP-712 signature through the CustomSource cast (F073) | new | #337 |
| F230 | 9 | wallet-hosted | packages/sdk/src/wallet/node/wallets/hosted/privy/PrivyWallet.ts:22,36,48-50 | low | info | CONSOLIDATED E2E SPEC (wallet-hosted slice): real Privy/Turnkey/Dynamic creds + Anvil signing must assert recoverMessageAddress===wallet.address===signer.address, exact-amount on-chain, Permit2 verifyTypedData, adversarial wrong-address (zero recover/verify assertions exist today) [P10 refined high/malicious-sign->low/info: e2e-spec, signing routes by walletId not caller address; no reachable malicious-sign; aligns with unit companion F231 low] | refines:self | #335 |
| F231 | 9 | wallet-hosted | packages/sdk/src/wallet/node/wallets/hosted/privy/__tests__/PrivyWallet.spec.ts:1 | low | info | No hosted wallet has a signer-address self-test at construction; the generic reconciliation seam (F074-refines) that would catch F028/F029/Turnkey-gap at once is untested for every provider | new | none |
| F232 | 9 | wallet-hosted | packages/sdk/src/wallet/node/providers/hosted/privy/__tests__/PrivyHostedWalletProvider.spec.ts:43-44 | low | malicious-sign | The toActionsWallet Privy address assertion is a second tautology locus (companion to F226): the empty MockPrivyClient cannot model walletId->key, so address-in equals address-out by construction | new | #335 |
| F233 | 9 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/__tests__/DefaultSmartWallet.spec.ts:27-36,128-263 | medium | correctness | Entire 4337 signing/dispatch path self-mocked (toCoinbaseSmartAccount + bundler + findSignerIndexOnChain stubbed); send/sendBatch assert the encoder against itself with no independent oracle; funds-moving primitive has no execution coverage [P10 refined high->medium: real coverage gap but headline repro inaccurate (ownerIndex regression IS caught); umbrella, remediation owned by F241/F242] | refines:self | #335 |
| F234 | 9 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/__tests__/DefaultSmartWallet.spec.ts:265-354,435-511 | medium | correctness | addSigner/removeSigner tests cannot fail on the stale in-memory owner-set bug (F087) or only-owner brick (F039): sendBatch + findSignerIndexOnChain mocked, post-rotation signing state never asserted | new | #163 |
| F235 | 9 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/utils/__tests__/findSignerIndexOnChain.spec.ts:20-22,45-48 | low | correctness | findSignerIndexOnChain test comments mislabel nextOwnerIndex as 'ownerCount'; no fixture has ownerCount<nextOwnerIndex (high-slot live owner after a low-slot removal), so an ownerCount-based regression passes | new | none |
| F236 | 9 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/__tests__/DefaultSmartWallet.spec.ts:178-185,251-258,533-554 | medium | correctness | Attribution-suffix tests assert concatHex against concatHex (no decode); no test proves the suffixed callData/initCode is a tolerable trailing-bytes form for the EntryPoint, leaving F063/F065 invisible (smart-wallet locus) | new | none |
| F237 | 9 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/__tests__/DefaultSmartWallet.spec.ts:68-96,98-126 | medium | fund-loss | No test recomputes the deterministic CREATE2 address from an independent oracle nor asserts factory-getAddress === real toCoinbaseSmartAccount(...).address; an owner-bytes/recipient mis-derivation relocating the funded address ships green | new | #131 |
| F238 | 9 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/__tests__/DefaultSmartWallet.spec.ts:162-169,236-242 | medium | correctness | send/sendBatch success-path tests hardcode receipt.success=true; no negative test for a reverted-but-mined userOp (receipt.success=false), so the F034 'revert reported as success' contract is unverified for the main dispatch methods | new | none |
| F239 | 9 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/utils/__tests__/findSignerInArray.spec.ts:9-68 | low | correctness | findSignerInArray suite has no test for a malformed/non-address owner (F090: getAddress throws inside findIndex) nor a WebAuthn-only owner set (F064: returns -1, constructor throws); both un-handled paths untested | new | #163 |
| F240 | 9 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/__tests__/DefaultSmartWallet.spec.ts:512-561 | low | fund-loss | sendTokens has no unit test at all in the smart-wallet suite; the poisoned-recipient (F035) and NaN/scientific-notation amount (F036/F041) paths on the one fund-moving builder are entirely uncovered | new | #379 |
| F241 | 9 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:196-294,310-422,456-500 | high | correctness | E2E-SPEC (smart-wallet leg): 4337 deploy + execute + owner-rotation (sign-with-new-key, only catch for F087) + suffix on a real bundler/Anvil with exact-amount and recipient-in-bytes/residual-allowance/only-owner/wrong-index adversarial cases | verified | #335 |
| F242 | 9 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/__tests__/DefaultSmartWallet.spec.ts:128-263 | low | correctness | No interim (no-bundler) test decodes the executeBatch callData with the real coinbaseSmartWalletAbi as an independent oracle to assert the inner (to,value,data) tuples, leaving F023/F108 unanchored until the full e2e (F241) lands | new | #335 |
| F243 | 9 | core-services | packages/sdk/src/utils/__tests__/validation.test.ts:1-51 | medium | correctness | Signing-path validators (validateSlippage/validateRecipient/validateNotZeroAddress/validateAmountPositiveIfExists/validateQuoteNotExpired) have ZERO unit tests; every known bypass (F110/F111/F066/F076/F006) ships green [P10 refined high/malicious-sign->medium/correctness: pure test-coverage; bypass impact owned by F110/F111/F066/F076] | refines:self | #303 |
| F244 | 9 | core-services | packages/sdk/src/utils/__tests__/validation.test.ts:1-51 | medium | correctness | The validation.test.ts file is the missing-coverage locus: it must grow per-validator intent-encoding describe blocks (Rule 9); today only resolveSupportedChainIds + validateWalletAddress are covered [P10 refined high->medium: structural twin of F243, strongest concrete bug config-gated, other bypasses weak] | refines:self | #373 |
| F245 | 9 | core-services | packages/sdk/src/utils/__tests__/approve.test.ts:261-294 | medium | correctness | approve.test.ts enshrines the F042 deficit-vs-set ERC20 bug: asserts the approval tx exists but never decodes its amount, locking in an under-approval and defending the broken path against fixes | new | #133 |
| F246 | 9 | core-services | packages/sdk/src/utils/__tests__/assets.test.ts:33-54 | medium | correctness | assets.test.ts never exercises the precision-loss / scientific-notation paths (F041) nor a formatAssetAmount round-trip; only clean small values (100, 1, 0.5) are tested | new | #379 |
| F247 | 9 | core-services | packages/sdk/src/actions/swap/providers/velodrome/__tests__/VelodromeSwapProvider.network.test.ts:131-167,262-291 | high | correctness | (core-services lens) The only swap network test is directional-only, never EXECUTES the swap, never recomputes amountOut independently, and never decodes the recipient out of swapCalldata (F075/#444 recipient-in-bytes) | verified | #444 |
| F248 | 9 | core-services | packages/sdk/src/actions/borrow/providers/morpho/__tests__/MorphoBorrowProvider.network.test.ts:181-260 | medium | correctness | (core-services lens) Morpho borrow network test asserts ranges/encoder-self-consistency, never executes the open bundle, decodes with the same blueAbi it encoded with, and never asserts amount/onBehalf (F086) | new | #334 |
| F249 | 9 | core-services | packages/sdk/src/utils/test.ts:323-392 | low | infra | Shared Anvil funding helper silently swallows USDC-funding failure (catch -> console.log, dead-on-arrival); chain-mismatched whale/USDC, float-truncated funding amount, and an `as any` signing client [P10 refined high->low: fundWallet/setupSupersimTest are dead code (zero callers, not exported); no current vacuous-pass path] | refines:self | #335 |
| F250 | 9 | core-services | packages/sdk/src/utils/test.ts:74-105 | medium | infra | Hardcoded fixed Anvil ports (18545/46/47) collide across parallel network suites, and the readiness probe accepts any HTTP 200 (incl. JSON-RPC error bodies / wrong-chain forks) as 'ready' | new | #332 |
| F251 | 9 | core-services | packages/sdk/src/services/__tests__/ChainManager.spec.ts:97-105,190-296 | low | infra | ChainManager.spec fully viem-mocked; the duplicate-chain test name lies (asserts 'is not supported' not the duplicate path) and the bundler client is never verified to bind the requested chainId/EntryPoint (F045) | new | #82 |
| F252 | 9 | core-services | packages/sdk/src/utils/__tests__/serializers.test.ts:1-53 | low | correctness | serializeBigInt tests miss the documented data-loss cases (Map/Set->{}, Date->string, undefined dropped) and the type-signature lie (bigint->string while return type claims T) on the CLI/HTTP serialization boundary (F044) | new | #419 |
| F253 | 9 | core-services | packages/sdk/src/services/__tests__/tokenBalance.spec.ts:29-143 | low | info | tokenBalance suite fully MockChainManager-driven with a constant defaultBalance; no fork test verifies real ERC20 decimals/aggregation, so a wrong-decimals or multicall-shape bug is invisible | new | #452 |
| F254 | 9 | core-services | packages/sdk/src/utils/test.ts:1-438 | low | infra | CONSOLIDATED E2E ANVIL SPEC (core-services synthesis): single OP-mainnet fork feature-test with real-cred signing, signer self-test, exact-amount oracles, ephemeral ports + chainId-validated readiness, and recipient-in-bytes/residual-allowance/quote-aging cases [P10 refined high->low: test-only harness, not exported/no production reach; validators already present + partly tested] | refines:self | #335 |
| F255 | 9 | core-services | packages/sdk/src/__tests__/index.exports.spec.ts:1-27 | low | infra | No snapshot guard on the ~141-symbol public export surface (only ~4 pinned, F147; two wildcard export *, F148); a refactor can silently drop/rename a safety-bearing error/validator symbol with green CI | new | #483 |
| F256 | 9 | core-services | packages/sdk/src/utils/assets.ts:44-55 | low | correctness | formatAssetAmount (the human-readable inverse of parseDecimalAmount) has zero direct test coverage, so its parseFloat lossiness (>2^53 base units) and negative-input padStart break ship green (companion to F246) | new | #379 |
| F152 | 8 | swap | packages/sdk/package.json (deps) / src/index.ts:8,13 | medium | infra | Swap-only consumers inherit ethers + @aave/* + @morpho-org/* as hard `dependencies` eagerly static-linked through the root barrel (no per-action subpath, no dynamic import); protocol-SDK sibling of F149/#131 — swap's only runtime import is viem | new | #131 |
| F153 | 8 | swap | packages/sdk/src/actions/swap/providers/uniswap/encoding.ts:1-30,224-294 | medium | correctness | All Uniswap/Velodrome router calldata hand-rolled on viem with no official-SDK differential oracle; the misleadingly-named sdk.test.ts validates the encoder against itself, leaving the F046/F047/F004/F005/F048 encoding cluster with no external reference anchor | new | #318 |
| F154 | 8 | swap | packages/sdk/src/actions/swap/providers/uniswap/addresses.ts:30-114; velodrome/addresses.ts:1-107 | low | info | Vendored router/poolManager/factory addresses that become the signed execute() target + Permit2 spend target are hardcoded with weak/no pinned-source provenance and no registry-match test; a one-char edit reroutes a signed swap with no CI tripwire | new | #328 |
| F155 | 8 | swap | pnpm-lock.yaml (viem@2.33.0); pnpm audit --prod | low | info | pnpm-audit/Wiz reconciliation: swap surface clean of the 53 high-severity advisories (all in hosted-wallet/Solana/react/demo closure); swap runtime is a single consistent viem@2.33.0, no skew, no advisory on viem/ethers | new | #432 |
| F156 | 8 | lend | packages/sdk/src/actions/lend/providers/aave/sdk.ts:1-3,118,121,151 | medium | infra | ethers@5 + @aave/contract-helpers + @aave/math-utils are runtime deps used ONLY on the Aave read path (getReserve display), never on signing (signing uses viem + local POOL_ABI); demotable to dev-only differential oracle | new | #255 |
| F157 | 8 | lend | packages/sdk/src/actions/lend/module.ts:1; index.ts:2; AaveLendProvider.ts:28 | medium | infra | Eager static import chain (module.ts->index.ts->AaveLendProvider->sdk.ts) forces the Aave/ethers@5/@aave protocol-SDK tree onto Morpho-only integrators; if(config.aave) gates construction not module evaluation (lend analog of #131/F149) | new | #131 |
| F158 | 8 | lend | packages/sdk/src/actions/lend/providers/aave/sdk.ts:3,118; pnpm-lock.yaml:10238 | medium | infra | Unpatched elliptic 6.6.1 CVE (GHSA-848j-6mx2-7j84, no fix) rides into the lend runtime via ethers@5 @ethersproject/signing-key 5.8.0, pulled by the Aave read path; demoting ethers@5 (F156) removes it | new | #432 |
| F159 | 8 | lend | packages/sdk/package.json (deps block) | low | infra | Protocol SDKs that build/validate calldata pinned with floating caret ranges (blue-sdk ^4.5.1->4.13.1 8 minors drift, blue-sdk-viem ^3.1.1 on the Morpho signing path); a fresh install without the lockfile can silently bump signing-path code | new | #255 |
| F160 | 8 | lend | packages/sdk/src/actions/lend/providers/morpho/MorphoLendProvider.ts:1,64,110 | low | correctness | Morpho signing-path calldata taken straight from third-party MetaMorphoAction.deposit/withdraw with no decodeFunctionData round-trip asserting args match amount/receiver/owner; asymmetric with the Aave sibling's local pinned ABI | new | #373 |
| F161 | 8 | lend | pnpm-lock.yaml:5004,5007,13498,13540 | low | info | Two ethers majors resolve in the lockfile (5.8.0 for @aave read path, 6.16.0 for @turnkey/core), doubling the ethers crypto surface; demoting ethers@5 (F156) converges the tree | new | none |
| F162 | 8 | borrow | packages/sdk/package.json (@morpho-org/* ^ ranges) | medium | infra | Signing-path Morpho math/ABIs (MarketUtils.getMarketId, AccrualPosition, blueAbi) ride on caret-floating @morpho-org/* runtime deps with no exact pin; an in-range bump shifts marketId/calldata/health silently AND verifyMorphoMarketId calls the same floating getMarketId so it shifts both sides and passes | new | #432 |
| F163 | 8 | borrow | packages/sdk/src/actions/borrow/providers/morpho/blue.ts:2,73,91,109,124 | low | info | @morpho-org/blue-sdk-viem is a full runtime dependency used ONLY for static ABI constants (blueAbi/blueOracleAbi/adaptiveCurveIrmAbi); vendorable/demotable to dev-only, mirroring the Aave borrow provider's vendored ABIs (blue-sdk math itself NOT demotable) | new | #328 |
| F164 | 8 | borrow | packages/sdk/src/actions/borrow/providers/aave/calldata.ts:1-8 | low | info | Aave borrow provider is viem-only at runtime (zero @aave/* or ethers imports); those three are pulled by the lend Aave read path, so @aave/* + ethers@5.7.2 are dead weight for borrow-only consumers (per-surface demotion evidence) | new | #131 |
| F165 | 8 | wallet-core | package.json:50-69 | medium | infra | viem signing-path version pinned to 2.33.0 only via workspace pnpm.overrides (not shipped); published SDK floats viem ^2.24.1 (and permissionless ^0.2.54), so consumers resolve an untested 2.x and sign with a viem build the SDK never tested | new | #131 |
| F166 | 8 | wallet-core | packages/sdk/package.json:65-73 (pnpm-lock autoInstallPeers) | low | infra | autoInstallPeers:true masks the missing peerDependenciesMeta.optional (F149 root): pnpm silently auto-installs all 10 vendor SDKs, so the broken single-vendor consumer install never reproduces in this repo's dev/CI | new | #131 |
| F167 | 8 | wallet-core | .github/workflows/build-images.yaml:1 | low | infra | No CI dependency audit (pnpm audit / OSV / dependency-review) and no --frozen-lockfile install outside release:publish; a malicious/vulnerable transitive update into the viem/permissionless signing graph would not be caught before publish | new | #432 |
| F168 | 8 | wallet-core | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:11 | low | info | Pass-closure note: wallet-core runtime third-party surface is viem-only (viem/account-abstraction/actions); all materially needed on the signing/dispatch hot path, nothing demotable; supply-chain risk is version-integrity (F165) + CI hygiene (F166/F167), not the import set | new | #283 |
| F169 | 8 | wallet-hosted | packages/sdk/package.json (peerDependencies) | medium | infra | All 10 signing-path vendor SDKs pinned with unbounded `>=` ranges (no upper bound), accepting ANY future major of a signer-construction SDK into a consumer's install; distinct from F149 (optionality/bundling) — this is the range-ceiling concern | new | #131 |
| F170 | 8 | wallet-smart | packages/sdk/package.json:67 | medium | infra | viem is a plain (non-peer) dep at floating ^2.24.1 while the deterministic CREATE2 address (toCoinbaseSmartAccount '1.1') and UserOp callData are fully delegated to viem AA internals; published float + dual-viem boundary can silently relocate the funds-receiving undeployed address and break receipt/type identity | new | #131 |
| F171 | 8 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/__tests__/DefaultSmartWallet.spec.ts:81,95,124 | medium | correctness | No golden-vector test pins the real viem-derived deterministic CREATE2 address (fixed owners+nonce) to a known constant; the suite only asserts version:'1.1' was passed and compares to mocked addresses, so an in-range viem bump relocating wallet addresses ships green | new | #131 |
| F172 | 8 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/constants/index.ts:1-482 | low | info | Hardcoded vendored Coinbase smart-wallet factory address + full ABI (trust root for createAccount/getAddress + owner-management signing) have no upstream-commit provenance pin and no per-chain on-chain integrity test; a drifted ABI/address would mis-encode or mis-derive | new | #90 |
| F173 | 8 | core-services | packages/sdk/package.json:64 | low | info | @morpho-org/morpho-ts declared as a direct runtime dependency but has zero source imports (only a vi.mock test stub); already a blue-sdk transitive, so it is pure first-order supply-chain bloat the published manifest misrepresents | new | none |
| F174 | 8 | core-services | packages/sdk/src/types/lend/morpho.ts:6-17 | low | info | @morpho-org/blue-sdk vault types re-exported into the public .d.ts via the types/index export-* chain, making blue-sdk a non-demotable hard type-resolution dep every consumer (incl. swap-only/EOA-only) must install to typecheck; couples public type contract to a fast-moving SDK | new | #209 |
| F175 | 8 | core-services | packages/sdk/src/services/ChainManager.ts:2-4 | low | info | permissionless value-imports (createSmartAccountClient/createPimlicoClient) statically loaded via the always-imported actions.ts->new ChainManager path for every consumer incl. EOA-only; materially needed (not demotable) but eager-bound, defeating lazy-load (F149 family) | new | #131 |
| F176 | 8 | core-services | packages/sdk/package.json:58-80 | low | infra | Manifest-wide umbrella: all runtime deps caret, all 10 peers unbounded >=, no peerDependenciesMeta.optional, no published lockfile; pinning rests on the non-shipped root pnpm.overrides. pnpm audit 74 high + 1 critical all resolve under demo-frontend/devDeps — NONE on a packages/sdk runtime path (SDK runtime CVE-clean), reconciling Wiz #432 | new | #432 |
| (refines:F149) | 8 | wallet-hosted | packages/sdk/src/wallet/node/index.ts:1,6; react/index.ts:5 | medium | infra | Lazy-load invariant asymmetrically violated: Turnkey is lazy-only but node barrel eagerly value-re-exports PrivyHostedWalletProvider/PrivyWallet (-> @privy-io/node) and react barrel DynamicWallet (-> @dynamic-labs/ethereum) through `export *`, so importing the SDK root statically pulls those vendors for Turnkey/Local-only consumers | refines:F149 | #131 |
| (refines:F146) | 8 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/error/errors.ts:1,5 | low | infra | Supply-chain angle on F146: the publicly-thrown SmartWalletDeploymentError.receipt + abstract SmartWallet signatures leak viem WaitForUserOperationReceiptReturnType, and because viem floats ^2.24.1 (non-peer, F170) the type identity is pinned to the nested viem and drifts from the consumer's; demotion-to-peer (F170) is the real fix | refines:F146 | #476 |
| F118 | 7 | swap | packages/sdk/src/actions/swap/core/SwapProvider.ts:118-125 | medium | correctness | permit2ExpirationSeconds read off _config via `as` cast; declared only on UniswapSwapProviderConfig, absent from base SwapProviderConfig + VelodromeSwapProviderConfig, so the signing-path Permit2-expiry override is type-invisible/silently swallowed on every non-Uniswap provider | new | #337 |
| F119 | 7 | swap | packages/sdk/src/types/swap/base.ts:136-149 | low | info | SwapPriceParams is exported public surface with zero references and no backing method (dead public API); sibling SwapPrice also exported and overlaps SwapQuote | new | #483 |
| F120 | 7 | swap | packages/sdk/src/types/swap/base.ts:79-103,154-173 | low | correctness | Exact-output is a per-provider capability with no discriminated-union/type-level surfacing; price-routing path silently drops Velodrome's ExactOutputNotSupportedError via Promise.allSettled | new | #440 |
| F121 | 7 | swap | packages/sdk/src/actions/swap/providers/velodrome/encoding/routers/v2.ts:101,203 | low | info | Velodrome v2 router-type switch throws bare `throw new Error('Unknown router type')` at two sites — the only two non-ActionsError throws in the swap surface (SDK side of #474) | new | #474 |
| F122 | 7 | swap | packages/sdk/src/actions/swap/index.ts:1-20 | low | info | No export-surface snapshot test for the swap public API (defaults-guard already covered by SwapProvider.test.ts); export-set drop ships green | new | #483 |
| F123 | 7 | lend | packages/sdk/src/types/lend/base.ts:373 | low | correctness | LendProviderMethods._closePosition return-type contract is wrong (Promise<TransactionData>) vs every implementation's Promise<LendTransaction>; exported type lies about the close-path shape | new | #209 |
| F124 | 7 | lend | packages/sdk/src/__tests__/index.exports.spec.ts:16-26 | low | info | Public export-surface guard test covers only borrow providers, leaving the lend re-exports (and the AaveLendProvider gap) unguarded | new | #483 |
| F125 | 7 | lend | packages/sdk/src/index.ts:13 | medium | info | Asymmetric public export: AaveLendProvider dropped from the package root index while LendProvider/MorphoLendProvider (and the whole borrow provider pair) are re-exported; root import resolves undefined | new | #483 |
| F126 | 7 | lend | packages/sdk/src/actions/lend/utils/markets.ts:70-76 | low | correctness | isMarketAsset/validateMarketAsset compares possibly-undefined per-chain addresses with raw ===, so two assets neither configured on the market chain validate as equal (and comparison is case-sensitive unlike lendMarketIdMatches) | new | #334 |
| F127 | 7 | lend | packages/sdk/src/types/lend/base.ts:146-157 | low | info | ApyBreakdown open-ended `[key: string]: number | undefined` index signature erases type precision on a public return type and lets undefined leak as a numeric field | new | #337 |
| F128 | 7 | borrow | packages/sdk/src/index.ts:92-93,134 | low | info | Public-export asymmetry: AaveBorrowMarketConfig exported but MorphoBorrowMarketConfig is not, so Morpho-allowlist integrators have no narrowed public type | new | #483 |
| F129 | 7 | borrow | packages/sdk/src/actions/borrow/namespaces/BaseBorrowNamespace.ts:38,40-49 | low | correctness | BaseBorrowNamespace.getMarkets documents `@throws ChainNotSupportedError` but Promise.allSettled+flatMap swallows it, contradicting the provider-level getMarkets which DOES throw (opposite contracts for unsupported-chain filter) | new | none |
| F130 | 7 | borrow | packages/sdk/src/actions/borrow/core/BorrowProvider.ts:277-286 | low | info | requireOwnMarket throws a bare Error, the only borrow throw outside the named-error taxonomy used everywhere else in the surface (SDK side of #474) | new | #474 |
| F131 | 7 | borrow | packages/sdk/src/types/borrow/quote.ts:112-113 | low | info | BorrowReceipt.positionAfter typed optional but dispatch always populates it from quote.positionAfter, advertising a weaker (and post-state-claiming) contract than delivered (type-design sharpening of F054) | new | none |
| F132 | 7 | wallet-core | packages/sdk/src/wallet/core/wallets/abstract/types/index.ts:30-42 | medium | correctness | TransactionReturnType/BatchTransactionReturnType are untagged unions, forcing every receipt consumer into fragile runtime shape-sniffing (Array.isArray + 'userOpHash' in receipt); API-design root under F072/F054 | new | #337 |
| F133 | 7 | wallet-core | packages/sdk/src/wallet/core/utils/extractReceiptHashes.ts:24-30 | low | correctness | extractReceiptHashes maps an empty EOA batch to {transactionHashes:[]} with no signal the batch produced no hash, and surfaces [undefined] for a malformed receipt | new | none |
| F134 | 7 | wallet-core | packages/sdk/src/wallet/core/wallets/abstract/Wallet.ts:176-198 | low | info | Abstract Wallet.send/sendBatch JSDoc says 'resolving to the transaction hash' but every implementation returns a full receipt (EOATransactionReceipt / receipt[] / WaitForUserOperationReceiptReturnType) | new | #367 |
| F135 | 7 | wallet-core | packages/sdk/src/wallet/core/utils/retryOnStaleRead.ts:21-38 | low | info | retryOnStaleRead final unconditional read is not wrapped, so an all-attempts-stale-then-final-throw propagates rather than returning the stale value the doc implies; no test covers that path (verified NOT an off-by-one) | new | none |
| F136 | 7 | wallet-core | packages/sdk/src/wallet/core/wallets/smart/abstract/types/index.ts:19 | low | info | Signer union mixes a non-signing Address with signing accounts under one type, with no type-level guard that a signing context actually got a signer (OwnerIdentifier vs SigningSigner split) | new | #337 |
| F137 | 7 | wallet-hosted | packages/sdk/src/wallet/react/providers/hosted/types/index.ts:60-65 | medium | correctness | Turnkey toActionsWallet options type diverges between node and react (client field present in react, absent in node) — inconsistent provider contract for the same provider key, no shared contract test | new | #330 |
| F138 | 7 | wallet-hosted | packages/sdk/src/wallet/node/providers/hosted/types/index.ts:60-64 | low | correctness | Turnkey signWith typed bare string for three distinct meanings (no discriminated union); a private-key-ID without ethereumAddress is a silently under-specified call the type system cannot catch | new | #475 |
| F139 | 7 | wallet-hosted | packages/sdk/src/wallet/react/wallets/hosted/dynamic/utils/createSigner.ts:22 | low | info | Hosted Dynamic createSigner throws bare Error ('Wallet not connected or not EVM compatible') while the rest of the SDK uses a named-error taxonomy; no WalletNotConnectedError/UnsupportedWalletError exists (SDK side of #474) | new | #474 |
| F140 | 7 | wallet-hosted | packages/sdk/src/wallet/node/index.ts:1-8 | low | info | Hosted public-export surface inconsistent across providers/environments: node exports only Privy classes, react only DynamicWallet, Turnkey classes unexported; no export-snapshot guard (#483/#131 on hosted) | new | #483 |
| F141 | 7 | wallet-hosted | packages/sdk/src/wallet/core/providers/hosted/abstract/HostedWalletProvider.ts:45-56 | low | info | Abstract createSigner/toActionsWallet share one TOptionsMap[TType] param type, but node providers feed createSigner construction-time deps merged via {...params} spread, so the abstract signature understates the real contract | new | #337 |
| F142 | 7 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/error/errors.ts:5-18 | medium | correctness | SmartWalletDeploymentError is the declared type of SmartWalletCreationResult.deployments[].error (public createSmartWallet return) and used for instanceof branching, yet never exported from src/index.ts; consumers cannot narrow it or recover chainId/receipt [P10 refined high->medium: real public-API export-gap but type-level/DX only (runtime data still readable); aligns sibling F143 medium] | refines:self | #474 |
| F143 | 7 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/error/errors.ts:5 | medium | correctness | SmartWalletDeploymentError extends bare Error, diverging from the ActionsError/BaseError taxonomy every other SDK error uses; breaks uniform instanceof handling on the deploy path | new | #474 |
| F144 | 7 | wallet-smart | packages/sdk/src/wallet/core/providers/smart/abstract/types/index.ts:7-12 | low | correctness | SmartWalletDeployment result models success as an untyped boolean rather than a discriminated union, so receipt/error presence is not type-enforced and consumers must defensively check both | new | #474 |
| F145 | 7 | wallet-smart | packages/sdk/src/wallet/core/providers/smart/default/DefaultSmartWalletProvider.ts:116-123 | low | correctness | createWallet re-wraps any non-SmartWalletDeploymentError rejection as a bare Error and throws it out of the allSettled aggregation, collapsing the per-chain partial-success contract and discarding other chains' settled results | new | #474 |
| F146 | 7 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/abstract/SmartWallet.ts:23-92 | low | info | Abstract SmartWallet contract leaks viem's WaitForUserOperationReceiptReturnType as its public send/sendBatch/deploy return type, coupling the SDK API to a vendor internal and hiding the receipt.success contract | new | #476 |
| F147 | 7 | core-services | packages/sdk/src/__tests__/index.exports.spec.ts:1-27 | medium | infra | Public-export surface has no snapshot/guard test: only 4 of ~141 exported symbols are pinned, so a refactor can silently drop/rename public API (incl. safety-default-bearing error/wallet symbols) with green CI (umbrella for the per-surface export-guard gaps) | new | #483 |
| F148 | 7 | core-services | packages/sdk/src/index.ts:77,167 | low | infra | Two wildcard `export *` re-exports of the error modules make the public error taxonomy uncurated: a new error class enters the public API with no review gate and a removed one breaks consumers silently | new | #474 |
| F149 | 7 | core-services | packages/sdk/package.json:peerDependencies | medium | infra | All 10 hosted-wallet vendor SDKs are hard peerDependencies with no peerDependenciesMeta.optional, and the published node/react barrels eagerly value-import Privy/Dynamic vendor code through static class re-exports, defeating the registry's lazy import() (#131 root cause) | new | #131 |
| F150 | 7 | core-services | packages/sdk/src/services/nameservices/ens/errors.ts:7,24,37 | low | correctness | Publicly-exported ENS errors (EnsNotConfiguredError/EnsResolutionError/EnsRpcError) extend bare Error not ActionsError, splitting the taxonomy and breaking the documented instanceof ActionsError discriminator on the recipient-resolution path | new | #474 |
| F151 | 7 | core-services | packages/sdk/src/types/borrow/params.ts:12,20 | low | correctness | Public amount-type contract inconsistent across sibling actions: borrow exposes precise Amount = {amount:number}|{amountRaw:bigint}, but lend (amount:number) and swap (amountIn?/amountOut?:number) offer no amountRaw bigint escape hatch (type-contract root of the F041 precision findings) | new | #379 |
| (refines:F114) | 7 | swap | packages/sdk/src/actions/swap/namespaces/BaseSwapNamespace.ts:81-111,244-275 | low | info | getQuotes public-method contract under-specified: JSDoc 'best first' is direction-blind (F114 face) and returned quotes are pre-built calldata produced WITHOUT the execute()-path validations | refines:F114 | #435 |
| (refines:F011) | 7 | lend | packages/sdk/src/actions/lend/providers/aave/AaveLendProvider.ts:79-83,117-118,206-208 | low | info | Lend providers throw bare Error from catch-alls and base buildLendApproval, collapsing the named ActionsError taxonomy (incl. dropped cause) on the signing path (API-contract framing, SDK side of #474) | refines:F011 | #474 |
| (refines:F102) | 7 | lend | packages/sdk/src/actions/lend/core/LendProvider.ts:141-154,84-118 | low | correctness | openPosition lacks the asset-check symmetry closePosition has, and getMarkets forwards a caller markets[] override verbatim that bypasses the allowlist prefilter (read-path twin of F081/F102) | refines:F102 | #334 |
| (refines:F101) | 7 | lend | packages/sdk/src/actions/lend/providers/morpho/api.ts:17-20,82-83 | low | info | Morpho GraphQL fetch returns any/Promise<any> at the protocol-SDK boundary with no response schema validation; public LendMarket.apy computed from an untyped boundary (#337 API-design framing of F101) | refines:F101 | #337 |
| (refines:F014) | 7 | lend | packages/sdk/src/actions/lend/providers/aave/sdk.ts:108-118 | low | info | Aave getReserve rebuilds a fresh ethers JsonRpcProvider from the chain's default/public RPC, so the public LendMarket read contract silently routes around the integrator's RPC choice (RPC-trust, info only) | refines:F014 | #211 |
| (refines:F021) | 7 | wallet-core | packages/sdk/src/wallet/core/wallets/eoa/EOAWallet.ts:62-100 | medium | correctness | EOA send/sendBatch never inspect receipt.status, so the abstract Wallet.send success contract means revert-as-error on the smart sibling and revert-as-success on EOA (cross-implementation contract gap, API-design framing of F021) | refines:F021 | #474 |
| (refines:F106) | 7 | wallet-core | packages/sdk/src/wallet/core/providers/WalletProvider.ts:106-117 | low | info | getSmartWallet throw-inside-try/console.error/rethrow anti-pattern; the thrown error is a bare Error (not named), so callers cannot discriminate bad-params from deploy/RPC failure (sharpens F106 with the named-error gap) | refines:F106 | #474 |
| (refines:F033) | 7 | wallet-hosted | packages/sdk/src/wallet/node/providers/hosted/registry/NodeHostedWalletProviderRegistry.ts:24-26,43-45 | medium | malicious-sign | Registry validateOptions is the only validation choke point and only checks one field's truthiness (react returns true unconditionally); signing-key selectors bypass it entirely, so the factory's `options is TOptions` contract advertises a no-op guard | refines:F033 | none |
| (refines:F073) | 7 | wallet-hosted | packages/sdk/src/wallet/react/wallets/hosted/privy/utils/createSigner.ts:27-28 | low | info | React Privy createSigner casts vendor signTypedData to CustomSource['signTypedData'] on the EIP-712/Permit2 seam with no recovering-signer test; only react Privy re-wraps and casts (residual any-style boundary cast, #337) | refines:F073 | #337 |
| (refines:F011) | 7 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:89-94 | low | info | Hot-path invariant violations (address getter, ensureLocalAccountSigner, addSigner else, getSignerPublicKey) throw bare Error strings instead of the named ActionsError taxonomy (SDK side of #474) | refines:F011 | #474 |
| (refines:F011) | 7 | core-services | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:244-248,288-292 | low | info | send/sendBatch collapse the underlying error class AND cause into a flat 'Failed to send transaction: ' + message string, discarding bundler/paymaster/nonce/revert signal on the main smart-wallet signing path | refines:F011 | #474 |
| F114 | 6 | swap | packages/sdk/src/actions/swap/namespaces/BaseSwapNamespace.ts:104-110,220-242 | medium | correctness | Best-quote routing always maximizes amountOutRaw, the wrong objective for exact-output swaps (should minimize amountInRaw cost); direction-blind comparator silently picks the costlier quote | new | none |
| F115 | 6 | swap | packages/sdk/src/actions/swap/providers/velodrome/encoding/helpers.ts:74-75 | low | info | buildSwapPrice computes price/priceInverse as float divisions yielding 'Infinity'/'NaN' strings for zero-or-tiny amounts, baked verbatim into the returned quote (display fields, unvalidated) | new | none |
| F116 | 6 | lend | packages/sdk/src/actions/lend/providers/aave/sdk.ts:173-176 | medium | correctness | Aave getReserve fills supply.totalAssets with idle availableLiquidity and supply.totalShares with borrow-side totalScaledVariableDebt; share-price/utilization is garbage and diverges from the Morpho sibling | new | #209 |
| F117 | 6 | borrow | packages/sdk/src/actions/borrow/providers/aave/presentation.ts:176,224 | medium | correctness | Aave maxLtv (and derived safeCeilingLtv) populated from the liquidation THRESHOLD bits, not the borrow-LTV bits (ltvBps decoded then discarded), overstating safe borrow capacity ~2-3pp and disagreeing with Morpho's maxLtv | new | none |
| (refines:F001) | 6 | swap | packages/sdk/src/actions/swap/core/SwapProvider.ts:286-298 | high | fund-loss | computeSlippageBounds has no [0,1) guard; slippage>=1.0 (reachable via getQuote skipping validateSlippage) yields a NEGATIVE amountOutMinRaw baked into signed router calldata (wrapped uint256 / opaque throw); negative slippage yields a revert-above-output min-out | refines:F001 | #318 |
| (refines:F041) | 6 | swap | packages/sdk/src/actions/swap/providers/uniswap/UniswapSwapProvider.ts:61-70 | medium | correctness | Raw-bigint _execute round-trips amountInRaw/amountOutRaw through parseFloat(formatUnits())→re-parse, dropping wei on >15-16 sig-digit 18-dec amounts and crashing with InvalidDecimalNumberError on tiny amounts that format to scientific notation | refines:F041 | #379 |
| (refines:F008) | 6 | lend | packages/sdk/src/actions/lend/core/LendProvider.ts:205-218 | medium | correctness | closePosition scales the withdraw amount by caller-supplied asset.metadata.decimals while validateMarketAsset checks address only; a wrong-decimals Asset mis-scales signed withdraw calldata (close-path twin of the open-path decimals hazard) | refines:F008 | #334 |
| (refines:F101) | 6 | lend | packages/sdk/src/actions/lend/providers/morpho/sdk.ts:472-491 | low | correctness | APY breakdown reverses performance fee via 1/(1-performanceFee) with no guard against fee>=1e18; on-chain fallback reads fee() raw, yielding Infinity/NaN/negative APY surfaced as apy.total and LendTransaction.apy | refines:F101 | #337 |
| (refines:F041) | 6 | lend | packages/sdk/src/utils/assets.ts:44-55 | low | correctness | formatAssetAmount round-trips a bigint through parseFloat (lossy for >2^53 base units / high-decimal balances) and breaks on negative input (padStart on a signed fractional string); not a faithful inverse of parseDecimalAmount | refines:F041 | #379 |
| (refines:F009) | 6 | lend | packages/sdk/src/actions/lend/core/LendProvider.ts:195-227 | low | correctness | closePosition has no positivity/finiteness check on params.amount before its own parse (215-218); a 0 amount yields a signable withdraw(0), NaN/negative throws opaque viem error instead of a named SDK error | refines:F009 | #303 |
| (refines:F015) | 6 | borrow | packages/sdk/src/actions/borrow/providers/morpho/close.ts:36-43 | low | correctness | Morpho close/_withdrawCollateral takes an explicit (non-max) collateral amount verbatim with no bound vs live collateral and no >0 guard; encodes a guaranteed-revert over-withdraw or a no-op withdrawCollateral(0) leg (asymmetric with the Aave resolveAaveAmount guards) | refines:F015 | none |
| (refines:F021) | 6 | wallet-core | packages/sdk/src/wallet/core/wallets/eoa/EOAWallet.ts:90-100 | high | fund-loss | EOA sendBatch mid-batch revert after a max-mode lend approval leaves a residual infinite allowance with zero deposit, reported as success (send() never checks receipt.status; smart sibling throws on !receipt.success) | refines:F021 | none |
| (refines:F054) | 6 | wallet-core | packages/sdk/src/actions/borrow/namespaces/WalletBorrowNamespace.ts:232-247 | medium | correctness | Borrow dispatch denormalizes quote.positionAfter/borrowAmount onto the receipt even when the underlying EOA tx reverted (no status reconciliation); positionAfter.healthFactor advertises a post-state that never materialized | refines:F054 | none |
| (refines:F023) | 6 | wallet-core | packages/sdk/src/wallet/core/providers/smart/default/DefaultSmartWalletProvider.ts:168-184 | medium | correctness | getWallet drops caller nonce AND defaults signers to [signer.address], so a retrieved multi-owner wallet signs with ownerIndex 0 / wrong owners and every UserOp is bundler-rejected on the documented getSmartWallet(addr,signer) path | refines:F023 | #163 |
| (refines:F074) | 6 | wallet-hosted | packages/sdk/src/wallet/node/wallets/hosted/privy/PrivyWallet.ts:22,36,48-50 | medium | malicious-sign | Node PrivyWallet is the only hosted wallet that sets this.address from caller input (params.address) instead of this.signer.address; reported address and signing key silently diverge (its own spec proves it) so approvals/balance/from/recipient target the wrong account [P10 refined high->medium: real + 3 siblings already self-set address from signer, but trigger is caller supplying inconsistent (walletId,address) = self-inflicted integration error, no external-attacker primitive; one-line fix] | refines:self | none |
| (refines:F039) | 6 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:390-422 | medium | correctness | removeSigner unconditionally encodes removeOwnerAtIndex and never reads ownerCount() or dispatches removeLastOwner (both in the bundled ABI); the only-owner removal path is unreachable and the LastOwner revert surfaces as an opaque failure | refines:F039 | #163 |
| (refines:F107) | 6 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/utils/findSignerIndexOnChain.ts:39-63 | low | correctness | Index scan is correctly driven by monotonic nextOwnerIndex (not ownerCount) returning the absolute slot, but the load-bearing invariant is undocumented and only enforced by a mislabeled test ('ownerCount'); a future ownerCount-based fix would skip live owners in high slots | refines:F107 | none |
| (refines:F041) | 6 | core-services | packages/sdk/src/utils/assets.ts:17-19,30-36 | medium | correctness | parseDecimalAmount/parseAssetAmount silently lose precision converting high-magnitude number amounts to signed bigint tx amounts (corruption before toString()); worst on the lend path which alone lacks a *Raw bigint escape hatch (distinct from F041's sci-notation THROW) [P10 refined high/fund-loss->medium/correctness: real silent IEEE-754 loss, lend number-only confirmed, but bounded to low-order sig digits (sub-ppm), no sign flip, no attacker amplification; user absorbs a few wei] | refines:self | #379 |
| (refines:F110) | 6 | core-services | packages/sdk/src/utils/validation.ts:111-115 | medium | correctness | validateSlippage has no slippage<=1 ceiling, so an integrator maxSlippage>1 admits slippage>100% that computeSlippageBounds turns into a NEGATIVE amountOutMinRaw, silently disabling slippage protection on a signed swap (validator-locus pair of the computeSlippageBounds negative-min-out) | refines:F110 | #373 |
| F097 | 5 | swap | packages/sdk/src/actions/swap/core/SwapProvider.ts:272-273,462-464 | medium | malicious-sign | Caller-supplied swap deadline never validated finite/positive/future before BigInt(deadline) is baked into signed router calldata (float→RangeError, 0/past→guaranteed-revert signed swap) | new | #373 |
| F098 | 5 | swap | packages/sdk/src/actions/swap/namespaces/BaseSwapNamespace.ts:131-137,266-275 | low | info | Provider iteration in getMarket/fetchAllQuotes swallows every provider error indistinguishably (allowlist/blocklist rejection vs RPC failure vs encoding throw), masking guard rejections | new | #334 |
| F099 | 5 | lend | packages/sdk/src/actions/lend/core/LendProvider.ts:84-118,234-257 | medium | malicious-sign | openPosition validates walletAddress but never runs marketId.address through isAddress; Morpho bakes it verbatim as the approval spender and tx to | new | #334 |
| F100 | 5 | lend | packages/sdk/src/actions/lend/providers/morpho/api.ts:73-89 | low | info | Morpho GraphQL fetch has no timeout/AbortController; a hung api.morpho.org stalls getVault and therefore open/close position flows indefinitely | new | #211 |
| F101 | 5 | lend | packages/sdk/src/actions/lend/providers/morpho/sdk.ts:538-579 | low | info | External Morpho GraphQL reward values (supplyApr/supplyAssetsUsd/fee) consumed as any with only ||0 coalescing; no finiteness/sign validation before flowing into displayed APY | new | #337 |
| F102 | 5 | lend | packages/sdk/src/actions/lend/core/LendProvider.ts:141-154 | low | info | getMarkets accepts a caller-supplied markets array that bypasses the allowlist prefilter; read path is fail-closed while the write path (F081) is fail-open | new | #334 |
| F103 | 5 | borrow | packages/sdk/src/actions/borrow/providers/aave/AaveBorrowProvider.ts:47-54,174-186 | medium | correctness | AaveBorrowProvider constructor never verifies marketId == computeAaveBorrowMarketId(chain,collateral,debt); spliced-reserve allowlist entry routes calldata against attacker tokens (Morpho sibling enforces this bind) [P10 refined high->medium: real + Morpho sibling enforces it, but malformed data is trusted developer allowlist config not attacker runtime arg; sibling F104 low] | refines:self | #334 |
| F104 | 5 | borrow | packages/sdk/src/actions/borrow/providers/aave/marketId.ts:14-25 | low | info | computeAaveBorrowMarketId hashes raw caller addresses without checksum/isAddress normalization; no exported verifyAaveMarketId helper to pair with the Morpho one | new | #328 |
| F105 | 5 | wallet-core | packages/sdk/src/wallet/core/providers/WalletProvider.ts:97-131 | medium | malicious-sign | getSmartWallet/getWallet never isAddress-validate caller-supplied walletAddress before it becomes the signed to: target of owner-management and deploy UserOps | new | #163 |
| F106 | 5 | wallet-core | packages/sdk/src/wallet/core/providers/WalletProvider.ts:106-117 | low | info | getSmartWallet validation branch uses throw-inside-try + console.error(error) + rethrow, emitting library-level logs (the only console.* in wallet/core) from an SDK boundary | new | #474 |
| F107 | 5 | wallet-core | packages/sdk/src/wallet/core/wallets/smart/default/utils/findSignerIndexOnChain.ts:39-65 | low | info | findSignerIndexOnChain runs an unbounded sequential read loop driven by nextOwnerIndex of a caller-influenced wallet address (self-DoS / latency amplification, no iteration cap) | new | none |
| F108 | 5 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/utils/formatPublicKey.ts:9-14 | medium | correctness | formatPublicKey silently passes non-address owner identifiers through verbatim into CREATE2 derivation and on-chain owner comparison with no length/shape validation | new | #163 |
| F109 | 5 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:196-207,261-294,456-500,573-587 | low | info | No smart-wallet entrypoint validates caller-supplied chainId against the configured chain set before it selects the signing client and the chain for address derivation/deploy/send (smart sibling of F022/F095) | new | none |
| F110 | 5 | core-services | packages/sdk/src/utils/validation.ts:111-115 | medium | correctness | validateSlippage admits NaN: slippage<0||slippage>max is false for NaN, so a NaN slippage bypasses the shared range guard | new | #303 |
| F111 | 5 | core-services | packages/sdk/src/utils/validation.ts:36-40 | low | correctness | validateAmountPositiveIfExists admits NaN/+Infinity: amount<=0 is false for both, so non-finite amounts bypass the shared positivity guard | new | #379 |
| F112 | 5 | core-services | packages/sdk/src/services/ChainManager.ts:198-202 | low | correctness | ChainManager validates config chainIds against viem's global chainById registry, not SUPPORTED_CHAIN_IDS; chain-scope rests on a bypassable TS type (producer-side of F095) | new | none |
| F113 | 5 | core-services | packages/sdk/src/services/nameservices/ens/EnsNamespace.ts:155-183 | low | info | EnsNamespace.getInfo returns attacker-controlled ENS text records (url/avatar/email/etc.) verbatim with no sanitization or doc warning (stored-XSS/SSRF seed for integrators) | new | #371 |
| (refines:F041) | 5 | swap | packages/sdk/src/utils/assets.ts:17-19,30-36 | medium | correctness | amount:number boundary forwards JS scientific-notation strings into viem parseUnits, throwing opaque InvalidDecimalNumberError on legitimate large (>=1e21) and tiny (<1e-6) swap amounts | refines:F041 | #303 |
| (refines:F001) | 5 | swap | packages/sdk/src/actions/swap/core/SwapProvider.ts:164-167,271,286-297 | low | malicious-sign | getQuote skips validateSlippage, so a slippage in (maxSlippage,1.0) bakes an arbitrarily-loose (or >=1.0, negative) minAmountOut into the returned quote's calldata at mint time | refines:F001 | #435 |
| (refines:F011) | 5 | lend | packages/sdk/src/actions/lend/providers/aave/AaveLendProvider.ts:79-83,117-119,205-209 | low | info | Aave open/close/getPosition catch-all interpolates caller-controlled amount and asset.metadata.symbol into thrown Error strings (log-injection / output-spoofing sink) | refines:F011 | #474 |
| (refines:F015) | 5 | borrow | packages/sdk/src/actions/borrow/providers/morpho/MorphoBorrowProvider.ts:190-229,231-272 | low | correctness | Morpho exact-amount depositCollateral/withdrawCollateral never reject a zero amount; a zero-value collateral leg is encoded and dispatched | refines:F015 | #303 |
| (refines:F092) | 5 | wallet-hosted | packages/sdk/src/wallet/core/namespace/WalletNamespace.ts:32 | low | correctness | isLocalAccount admits accounts missing signTypedData (optional clause); EIP-712/Permit2/credit-delegation signing fails at sign time instead of at wallet construction | refines:F092 | none |
| (refines:F011) | 5 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:243-249,287-293,494-499 | low | info | send/sendBatch/deploy catch blocks interpolate an unredacted bundler/paymaster error.message into a thrown string, the one place an external error string (potentially echoing signed callData/initCode) reaches the caller | refines:F011 | #474 |
| (refines:F066) | 5 | core-services | packages/sdk/src/utils/validation.ts:176-180 | medium | malicious-sign | Recipient resolver (strict:false) vs validateRecipient (strict) mismatch: zero-address guard silently no-ops on the exact non-checksummed inputs the resolver passes through into calldata | refines:F066 | #437 |
| F078 | 4 | swap | packages/sdk/src/actions/swap/providers/velodrome/VelodromeSwapProvider.ts:218-223 | medium | correctness | Velodrome _buildApprovals keys the allowance check to quote.recipient, not the signing wallet/token payer (Velodrome sibling of F002) | new | #436 |
| F079 | 4 | swap | packages/sdk/src/actions/swap/providers/velodrome/encoding/routers/v2.ts:253-273 | low | malicious-sign | v2/leaf router encoders bake caller recipient into signed calldata with no isAddress/checksum validation (diverges from universal/CL sentinel) | new | #437 |
| F080 | 4 | lend | packages/sdk/src/actions/lend/namespaces/WalletLendNamespace.ts:89-98 | medium | malicious-sign | Lend wallet-dispatch has no last-line chain/recipient reconciliation that its borrow sibling enforces before signing | new | #477 |
| F081 | 4 | lend | packages/sdk/src/actions/lend/core/LendProvider.ts:234-257 | medium | malicious-sign | Empty/undefined marketAllowlist makes validateMarketAllowed a no-op; open/close build approve+deposit to arbitrary caller-supplied marketId.address (fail-open) | new | #334 |
| F082 | 4 | borrow | packages/sdk/src/actions/borrow/namespaces/WalletBorrowNamespace.ts:211-237 | high | malicious-sign | Borrow recipient/onBehalf guard compares quote.recipient to a single cached wallet.address but dispatches on quote.marketId.chainId; no chain-to-address reconciliation for smart wallets [P10 demoted: DefaultSmartWallet address is provably chain-invariant (single factory/owners/nonce/version); guard already compares the actual per-chain account] | demoted | #163 |
| F083 | 4 | borrow | packages/sdk/src/actions/borrow/providers/aave/calldata.ts:19-39 | medium | correctness | Aave borrow encodes onBehalfOf=user but never establishes credit delegation; signer!=onBehalfOf reverts or mis-attributes debt with no SDK conformance check | new | #334 |
| F084 | 4 | borrow | packages/sdk/src/actions/borrow/namespaces/WalletBorrowNamespace.ts:232-237 | medium | correctness | Dispatch chainId taken from caller-influenced quote.marketId.chainId, passed to wallet.send with no reconciliation against the wallet's intended chain (sign-for-A/broadcast-on-B) | new | none |
| F085 | 4 | borrow | packages/sdk/src/actions/borrow/namespaces/WalletBorrowNamespace.ts:178-195 | low | malicious-sign | Raw-params re-quote borrow path injects wallet.address but never runs the recipient/onBehalf conformance check the pre-built path runs | new | #477 |
| F086 | 4 | borrow | packages/sdk/src/actions/borrow/providers/morpho/blue.ts:81-97 | low | correctness | Morpho borrow/withdraw encode receiver=onBehalf=walletAddress with no isAuthorized/setAuthorization conformance leg; signer-must-equal-onBehalf unstated/untested | new | #334 |
| F087 | 4 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:310-375,390-422,78-80,196-207 | low | correctness | addSigner/removeSigner never refresh in-memory signers/signerIndex/_address after a successful owner rotation; subsequent signs use the stale owner set [P10 refined high->low: real state-staleness but index-shift/owner-array-affects-signature premise is wrong (monotonic indices, deployed path signs correctly); narrow counterfactual/hygiene impact] | refines:self | #163 |
| F088 | 4 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:310-375,390-422,196-207 | medium | correctness | Owner rotation applied to a single chainId but wallet is CREATE2-deterministic across all chains; owner sets silently diverge (rotation is chain-local) | new | #163 |
| F089 | 4 | wallet-core | packages/sdk/src/wallet/core/wallets/eoa/EOAWallet.ts:33-73 | medium | malicious-sign | EOAWallet.send sets chain on the wallet client, making viem SKIP eth_chainId verification of the connected RPC node (sign-for-chainA/broadcast-on-chainB) | new | none |
| F090 | 4 | wallet-core | packages/sdk/src/wallet/core/wallets/smart/default/utils/findSignerInArray.ts:17-29 | low | correctness | findSignerInArray calls getAddress() inside findIndex, throwing InvalidAddressError on a malformed signers[] entry instead of returning -1 | new | #163 |
| F091 | 4 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:310-348 | low | correctness | addSigner does not isAddress-validate an EOA string signer before encoding addOwnerAddress; a malformed owner becomes a permanent on-chain owner slot | new | none |
| F092 | 4 | wallet-core | packages/sdk/src/wallet/core/namespace/WalletNamespace.ts:22-34,190-206 | low | info | isLocalAccount type-guard accepts any object with type:'local'+function fields; no address/checksum or signer-capability validation before wrapping into a signing wallet | new | none |
| F093 | 4 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:310-375 | medium | correctness | addSigner has no duplicate/idempotency guard and never reconciles the returned on-chain index with the owner it actually encoded | new | #163 |
| F094 | 4 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:196-207 | low | info | getCoinbaseSmartAccount omits entryPoint (relies on viem default) while constants ABI declares v0.6 UserOperation struct; no assert userOpHash chain == broadcast chain | new | #82 |
| F095 | 4 | core-services | packages/sdk/src/services/ChainManager.ts:162-164 | low | correctness | getChain returns chainById[chainId] with no membership check; it feeds the EIP-155 chain bound into EOA signatures (producer-side of F022) | new | none |
| F096 | 4 | core-services | packages/sdk/src/services/nameservices/ens/EnsNamespace.ts:19-20,185-194 | low | info | ENS recipient resolution silently falls back to a hardcoded third-party RPC (cloudflare-eth.com) when mainnet is not configured | new | none |
| (refines:F070) | 4 | swap | packages/sdk/src/actions/swap/core/SwapProvider.ts:434-438 | medium | malicious-sign | Pre-built quote dispatch signs execution.value (native msg.value) verbatim with no reconciliation to encoded amountIn / native-in flag | refines:F070 | #373 |
| (refines:F070) | 4 | swap | packages/sdk/src/actions/swap/core/SwapProvider.ts:434-451 | low | malicious-sign | Pre-built quote path trusts quote.chainId metadata for both validation and dispatch chain; never re-derives expected router/token addresses for that chain | refines:F070 | #373 |
| (refines:F052) | 4 | swap | packages/sdk/src/actions/swap/namespaces/WalletSwapNamespace.ts:69-101 | low | correctness | requireQuoteForThisWallet binds a pre-built quote by recipient only; input-token payer (msg.sender) never asserted to be this wallet | refines:F052 | #437 |
| (refines:F008) | 4 | lend | packages/sdk/src/actions/lend/providers/aave/AaveLendProvider.ts:56-84,92-120 | low | correctness | Aave open/close branch native-vs-ERC20 on different inputs (open uses caller params.asset, close uses market-driven marketInfo.asset); unreconciled leg-selection asymmetry | refines:F008 | #334 |
| (refines:F039) | 4 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:390-411 | high | fund-loss | removeSigner uses removeOwnerAtIndex with no ownerCount>1 guard and no not-self guard; can brick the account (LastOwner) or remove the only key this client can sign with [P10 verified at high: load-bearing harm is remove-only-key lockout (ownerCount>1, leaving only unsignable owners) = loss-of-access; LastOwner brick is contract-prevented; self-inflicted single-call footgun so high not critical] | verified | #163 |
| (refines:F038) | 4 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:322-335,400-407 | medium | correctness | WebAuthn owner add (decodeAbiParameters x||y) vs lookup/remove (formatPublicKey pass-through) derive owner bytes two ways with no assertion they match the on-chain representation | refines:F038 | #163 |
| (refines:F074) | 4 | wallet-hosted | packages/sdk/src/wallet/core/wallets/eoa/EOAWallet.ts:44-52 | medium | malicious-sign | No hosted wallet performs a signer-address self-test; reported .address vs actual signing key never verified for any provider (generic seam that would catch F028/F029/Turnkey gap) | refines:F074 | none |
| (refines:F031) | 4 | wallet-hosted | packages/sdk/src/wallet/node/wallets/hosted/turnkey/utils/createSigner.ts:25-31 | medium | malicious-sign | Turnkey caller-supplied ethereumAddress becomes the reported signing address with no reconciliation against the resolved signWith key (node+react); shortcut bypasses the API round-trip | refines:F031 | none |
| (refines:F033) | 4 | wallet-hosted | packages/sdk/src/wallet/node/providers/hosted/registry/NodeHostedWalletProviderRegistry.ts:41-58 | low | malicious-sign | Node Turnkey registry validateOptions only checks client truthiness; the signing-key selectors (organizationId/signWith/ethereumAddress) bypass every validation choke point | refines:F033 | none |
| (refines:F062) | 4 | wallet-hosted | packages/sdk/src/wallet/react/wallets/hosted/dynamic/utils/createSigner.ts:24-37 | low | malicious-sign | Dynamic signer mixes two backends (connector signRawMessage vs walletClient) with no address reconciliation and no connector capability guard | refines:F062 | none |
| (refines:F045) | 4 | core-services | packages/sdk/src/services/ChainManager.ts:90-127,244-273 | low | malicious-sign | getBundlerClient/getPimlicoBundlerClient never assert the supplied SmartAccount's chain/EntryPoint match the requested chainId before it becomes the userOp-signing client | refines:F045 | none |
| (refines:F076) | 4 | core-services | packages/sdk/src/utils/validation.ts:100-109,60-64 | low | malicious-sign | validateWalletAddress (shared signer-address guard) inherits the lowercase-literal zero-address bypass and never confirms the address is canonical or equals the actual signer | refines:F076 | #477 |
| F070 | 3 | swap | packages/sdk/src/actions/swap/core/SwapProvider.ts:434-438 | high | malicious-sign | Pre-built SwapQuote dispatch signs execution.routerAddress+swapCalldata+value verbatim; only metadata validated, no router allowlist/calldata re-derive [P10 verified at high: buildSwapTransactions copies routerAddress/swapCalldata/value verbatim; requireQuoteForThisWallet checks only recipient (different field); no router-allowlist or calldata re-derive; full wallet drain reachable via public wallet.swap.execute(quote)] | verified | #373 |
| F071 | 3 | lend | packages/sdk/src/actions/lend/core/LendProvider.ts:84-118 | high | malicious-sign | Exported LendProvider open/closePosition encode caller walletAddress verbatim as onBehalfOf/receiver/owner/to; dispatch never reconciles vs signer [P10 demoted: provider methods hold no signer (return unsigned calldata); WalletLendNamespace forces walletAddress=this.wallet.address spread AFTER params (no override); reconcile-vs-signer is the F074 seam (out of scope here), no opaque-quote dispatch unlike borrow F054] | demoted | #477 |
| F072 | 3 | wallet-core | packages/sdk/src/wallet/core/utils/executeTransactionBatch.ts:25-37 | medium | malicious-sign | executeTransactionBatch is the shared verbatim-signing choke point: zero calldata validation, lend guards nothing (generalizes F054) [P10 refined high->medium: only the borrow leg has a verbatim-quote dispatch (swap re-derives via provider.execute; lend has no caller-supplied calldata path); that borrow risk is owned high by F054; executeTransactionBatch lacks context to do the reconciliation (fix-here infeasible)] | refines:self | #373 |
| F073 | 3 | wallet-hosted | packages/sdk/src/wallet/react/wallets/hosted/privy/utils/createSigner.ts:22-29 | medium | malicious-sign | React Privy createSigner re-wraps a valid viem account and casts signTypedData (EIP-712/Permit2 seam); no recovering-signer test | new | none |
| F074 | 3 | wallet-hosted | packages/sdk/src/wallet/core/providers/hosted/abstract/HostedWalletProvider.ts:45-56 | medium | malicious-sign | No shared signer-identity reconciliation seam; each hosted provider trusts caller/vendor .address verbatim before verbatim-signing send/sendBatch | new | none |
| F075 | 3 | core-services | packages/sdk/src/core/error/errors.ts:330-350 | low | malicious-sign | QuoteRecipientMismatchError contract (shared pre-built-quote guard) trusts quote.recipient metadata, never the signed calldata bytes (root of F054) [P10 refined high->low: underlying gap real but filed at the error-CLASS definition (not defective code); the exploitable consumers are already filed high as F070 (swap) + F054 (borrow); counting this independently triple-counts the same exploit] | refines:self | #373 |
| F076 | 3 | core-services | packages/sdk/src/utils/validation.ts:60-64 | low | correctness | validateNotZeroAddress uses lowercase-literal === instead of isAddressEqual; non-canonical zero representation can bypass the guard | new | none |
| F077 | 3 | core-services | packages/sdk/src/actions.ts:250-267 | low | infra | createWalletProvider dereferences config.smartWalletConfig.provider inside the !config.smartWalletConfig branch; TypeError on optional-config path | new | none |
| (refines:F046) | 3 | swap | packages/sdk/src/actions/swap/providers/uniswap/encoding.ts:224-294 | high | fund-loss | V4 calldata uses TAKE_ALL with no recipient param; recipient field destructured but unused, output hard-coded to msg.sender [P10 verified at high alongside base F046] | verified | #444 |
| (refines:F047) | 3 | swap | packages/sdk/src/actions/swap/providers/velodrome/VelodromeSwapProvider.ts:194 | medium | correctness | Velodrome sets value=amountInRaw for native-in on all router types, but universal/CL encoders have no native branch; native-in strands/reverts [P10 refined high/fund-loss->medium/correctness alongside base F047: testnet-only universal/CL, revert-with-refund dominant outcome] | refines:self | none |
| (refines:F004) | 3 | swap | packages/sdk/src/actions/swap/providers/uniswap/UniswapSwapProvider.ts:172 | medium | fund-loss | Native-in exact-output attaches 1-unit placeholder value, not the encoded amountInMaximum; SETTLE_ALL underfunds and reverts | refines:F004 | none |
| (refines:F005) | 3 | swap | packages/sdk/src/actions/swap/providers/uniswap/encoding.ts:250-294 | medium | correctness | Encoder recomputes min-out/max-in with its own rounding vs computeSlippageBounds; displayed vs enforced protection diverge | refines:F005 | #318 |
| (refines:F002) | 3 | swap | packages/sdk/src/actions/swap/core/SwapProvider.ts:82-105 | medium | fund-loss | _buildApprovals passes quote.recipient as walletAddress, so Permit2 allowance owner is the recipient not the executing signer | refines:F002 | #436 |
| (refines:F050) | 3 | swap | packages/sdk/src/actions/swap/core/SwapProvider.ts:333-392 | medium | malicious-sign | Permit2 sub-approval expiration decoupled from swap deadline (30d default), max-mode grants maxUint160 standing spend authorization | refines:F050 | #436 |
| (refines:F052) | 3 | swap | packages/sdk/src/actions/swap/namespaces/WalletSwapNamespace.ts:40-55 | low | correctness | requireQuoteForThisWallet only constrains metadata recipient; no-op on Uniswap (msg.sender) and Velodrome universal/CL (sentinel) | refines:F052 | #437 |
| (refines:F001) | 3 | swap | packages/sdk/src/actions/swap/namespaces/BaseSwapNamespace.ts:53-79 | low | info | Read-only getQuote returns fully-built calldata with UNIVERSAL_ROUTER_MSG_SENDER sentinel recipient; can leak into provider.execute | refines:F001 | #435 |
| (refines:F051) | 3 | swap | packages/sdk/src/actions/swap/providers/uniswap/encoding.ts:257-293 | low | correctness | V4 amount/minOut/maxIn encoded into uint128 params with no <= maxUint128 bound check; over-range value wraps in signed calldata | refines:F051 | none |
| (refines:F049) | 3 | swap | packages/sdk/src/actions/swap/providers/velodrome/encoding/helpers.ts:60-83 | low | correctness | Every Velodrome/Aerodrome quote hard-codes priceImpact:0, disabling price-impact/sandwich-warning logic provider-wide | refines:F049 | none |
| (refines:F001) | 3 | swap | packages/sdk/src/actions/swap/core/SwapProvider.ts:164-167 | low | info | Provider-level getQuote() skips validateSwapExecute entirely; quote can encode calldata for inputs execute() would reject | refines:F001 | #435 |
| (refines:F008) | 3 | lend | packages/sdk/src/actions/lend/core/LendProvider.ts:100-106 | medium | fund-loss | openPosition never calls validateMarketAsset; mismatched caller asset still emits max-mode approve to pool/vault, survives reverted deposit | refines:F008 | #334 |
| (refines:F008) | 3 | lend | packages/sdk/src/actions/lend/providers/morpho/MorphoLendProvider.ts:48-84 | medium | correctness | Morpho _openPosition fetches vault but never compares vaultInfo.asset to params.asset; ERC4626 deposit omits asset (open/close asymmetry) | refines:F008 | #334 |
| (refines:F011) | 3 | lend | packages/sdk/src/actions/lend/providers/aave/AaveLendProvider.ts:79-83 | medium | correctness | Aave open/close catch-all rethrows generic Error, masking allowlist MarketNotAllowedError and asset-mismatch reverts on the signing path | refines:F011 | #474 |
| (refines:F009) | 3 | lend | packages/sdk/src/actions/lend/core/LendProvider.ts:84-90 | low | correctness | Lend open/close never reject non-positive/NaN/non-finite amount; 0/negative produces a still-signable position tx (swap sibling validates) | refines:F009 | #303 |
| (refines:F010) | 3 | lend | packages/sdk/src/actions/lend/core/LendProvider.ts:200-201 | low | info | marketBlocklist still declared in LendProviderConfig but no lend path enforces it; only allowlist checked (F010 survives in current tree) | refines:F010 | #334 |
| (refines:F054) | 3 | borrow | packages/sdk/src/actions/borrow/namespaces/WalletBorrowNamespace.ts:207-247 | high | malicious-sign | Pre-built borrow quote dispatch signs execution.transactions verbatim; four metadata checks never constrain the calldata bytes (concrete exploit) [P10 verified at high alongside base F054] | verified | #373 |
| (refines:F054) | 3 | borrow | packages/sdk/src/actions/borrow/core/validations.ts:83-102 | medium | malicious-sign | validateBorrowMarketIdInAnyAllowlist proves marketId allowlisted but never binds it to calldata target/approval spender | refines:F054 | #334 |
| (refines:F054) | 3 | borrow | packages/sdk/src/actions/borrow/namespaces/WalletBorrowNamespace.ts:238-246 | low | correctness | Borrow receipt envelope reports amounts/positionAfter from quote metadata, not from the dispatched calldata or a post-exec read | refines:F054 | none |
| (refines:F015) | 3 | borrow | packages/sdk/src/actions/borrow/core/internalParams.ts:129-140 | medium | correctness | Borrow amounts have no positivity/finiteness validation; raw-bigint path forwards 0/negative straight into calldata (swap sibling validates) | refines:F015 | #303 |
| (refines:F055) | 3 | borrow | packages/sdk/src/actions/borrow/core/quote.ts:54 | low | correctness | safeCeilingLtv surfaced as a safety value but never enforced against the projected position LTV on open/borrow | refines:F055 | none |
| (refines:F053) | 3 | borrow | packages/sdk/src/actions/borrow/providers/morpho/blue.ts:165-176 | low | correctness | Morpho max-repay/close in 'max' mode grants unbounded maxUint256 loan-token allowance directly to Morpho Blue (exact-mode bounds it) | refines:F053 | #133 |
| (refines:F061) | 3 | wallet-core | packages/sdk/src/wallet/core/wallets/eoa/EOAWallet.ts:62-100 | medium | malicious-sign | EOAWallet + DefaultSmartWallet send/sendBatch are public open-ended verbatim-signing surfaces with no recipient/value/allowlist/validate hook | refines:F061 | #414 |
| (refines:F023) | 3 | wallet-core | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:196-207 | medium | malicious-sign | Retrieved smart wallet signs UserOps against a caller-asserted owner set/index never reconciled with on-chain owners | refines:F023 | #163 |
| (refines:F037) | 3 | wallet-core | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:224-236 | medium | correctness | send/sendBatch forward only suffixed callData/initCode; gas+paymaster from explicit prep dropped, viem re-preps on suffixed bytes (sponsored != signed) | refines:F037 | #456 |
| (refines:F022) | 3 | wallet-core | packages/sdk/src/wallet/core/wallets/eoa/EOAWallet.ts:62-100 | low | correctness | EOA send/sendBatch never pin chainId into the signed tx; calldata-vs-chain mismatch signed against wrong network (distinct from config-membership) | refines:F022 | none |
| (refines:F035) | 3 | wallet-core | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:512-561 | low | malicious-sign | sendTokens does not isAddress-validate recipientAddress (falsy-only); malformed/poisoned recipient encoded straight into signed transfer | refines:F035 | #379 |
| (refines:F023) | 3 | wallet-core | packages/sdk/src/wallet/core/providers/WalletProvider.ts:97-131 | low | correctness | getSmartWallet derives address from deploymentSigners but signs with separate unreconciled signers array; address vs signing owner set can disagree | refines:F023 | #163 |
| (refines:F063) | 3 | wallet-core | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:594-598 | low | correctness | appendAttributionSuffix mutates bundler-returned callData/initCode with no inert-suffix assertion and no EntryPoint v0.7 guard | refines:F063 | none |
| (refines:F031) | 3 | wallet-hosted | packages/sdk/src/wallet/node/wallets/hosted/turnkey/utils/createSigner.ts:25-31 | low | malicious-sign | Turnkey signWith (key-selector deciding which key signs) forwarded to createAccount with zero validation in node and react | refines:F031 | none |
| (refines:F033) | 3 | wallet-hosted | packages/sdk/src/wallet/react/providers/registry/ReactHostedWalletProviderRegistry.ts:24-60 | low | info | React hosted registry validateOptions are unconditional return true; no centralized validation choke point on the react signing path | refines:F033 | none |
| (refines:F061) | 3 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:217-294 | medium | malicious-sign | send/sendBatch are unconstrained verbatim-signing primitives: no recipient allowlist, value cap, or calldata sanity (smart-wallet umbrella) [P10 refined high->medium: in-scope defense-in-depth floor (isAddress/zero/neg) but that floor doesn't stop the headline poisoned-look-alike exploit; raw escape hatch whose `to` is caller-chosen; aligns EOA refines:F061 (medium) + sendTokens refines:F035 (medium)] | refines:self | none |
| (refines:F037) | 3 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:224-236,266-281 | low | correctness | send/sendBatch sign the re-prepared op not the prepared one; second independent prepare (nonce/gas/fees/paymaster) can diverge from inspected op [P10 refined high->low: viem re-prepares over the SUFFIXED callData so the signed op is internally self-consistent (gas matches its own bytes); first uo is never surfaced/signed; no under-gas/fund-loss/malicious-sign; reduces to original F037 wasted-prepare (low)] | refines:self | #456 |
| (refines:F063) | 3 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:231-234,277-279,594-598 | medium | correctness | appendAttributionSuffix mutates SIGNED execute/executeBatch+initCode bytes with no decode-safety/EntryPoint-version assert at the call site | refines:F063 | none |
| (refines:F039) | 3 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:390-411 | medium | correctness | removeSigner derives index and ownerBytes independently; stale/wrong index can revert or remove the wrong live owner after index-shift | refines:F039 | #163 |
| (refines:F023) | 3 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:196-207,76-87,122 | medium | correctness | getCoinbaseSmartAccount signs with ownerIndex from caller signers array order, never reconciled with on-chain owner layout; wrong-slot sig reverts [P10 refined high->medium: real (addSigner/removeSigner reconcile via findSignerIndexOnChain but the send path doesn't), but wrong ownerIndex only makes validation FAIL (never accept as another owner) -> recoverable usability/DoS, no fund-loss/malicious-sign] | refines:self | #163 |
| (refines:F035) | 3 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:512-561 | medium | fund-loss | sendTokens falsy-only recipient check + JS number amount; poisoned recipient and float precision loss both flow into a signed transfer | refines:F035 | #379 |
| (refines:F034) | 3 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:261-294,217-250 | high | correctness | send/sendBatch return the UserOp receipt without inspecting receipt.success; reverted-but-mined inner call reported as success (main dispatch path) | refines:F034 | none |
| (refines:F058) | 3 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:573-587,196-207 | medium | correctness | Counterfactual address from getSupportedChains()[0] + nonce default 0; a dropped caller nonce makes send() operate on a different funded address | refines:F058 | #98 |
| (refines:F064) | 3 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/utils/findSignerInArray.ts:13-30 | low | correctness | findSignerInArray returns -1 for WebAuthn-only owner sets; legitimate passkey wallets cannot be constructed, leaving the WebAuthn path untested | refines:F064 | #163 |
| (refines:F038) | 3 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:322-326 | low | correctness | addSigner WebAuthn decodes publicKey as two bytes32 with no length check; a 65-byte uncompressed key mis-splits into a wrong permanent owner | refines:F038 | none |
| (refines:F011) | 3 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:243-249,287-293 | low | correctness | send/sendBatch catch{} flattens paymaster/signature/nonce/bundler/revert failures into one opaque "Failed to send transaction" string | refines:F011 | #474 |
| (refines:F043) | 3 | core-services | packages/sdk/src/services/nameservices/ens/utils.ts:43-47 | medium | malicious-sign | resolveAddress returns hex recipient verbatim with strict:false (no EIP-55 checksum); corrupted recipient flows into signed Velodrome calldata | refines:F043 | #371 |
| (refines:F068) | 3 | core-services | packages/sdk/src/services/nameservices/ens/EnsNamespace.ts:78-87 | medium | malicious-sign | EnsNamespace.getAddress caches ENS->address 5min with no invalidation/round-trip; stale/changed resolver record yields a wrong signed recipient | refines:F068 | #453 |
| (refines:F066) | 3 | core-services | packages/sdk/src/utils/validation.ts:176-180 | medium | malicious-sign | validateRecipient (sole recipient guard on pre-built swap path) only zero-checks; no reconciliation against the encoded calldata recipient | refines:F066 | #437 |
| (refines:F067) | 3 | core-services | packages/sdk/src/utils/approve.ts:107-130 | low | malicious-sign | buildPermit2ApprovalTx encodes spender/amount with no non-zero-spender / uint160 / uint48 bounds (spender+amount legs of the Permit2 payload) | refines:F067 | none |
| (refines:F045) | 3 | core-services | packages/sdk/src/services/ChainManager.ts:90-127 | low | info | getBundlerClient returns its client without binding to a verified chainId; bundler-returned UserOp calldata signed with no eth_chainId reconciliation | refines:F045 | #82 |
| F046 | 2 | swap | packages/sdk/src/actions/swap/providers/uniswap/encoding.ts:224-294 | high | fund-loss | Uniswap V4 swap calldata ignores recipient entirely — output always goes to msg.sender (TAKE_ALL, no TAKE 0x0e) [P10 verified at high: recipient param dropped, TAKE_ALL credits msg.sender, reachable via raw-params execute() with no recipient==wallet guard; broken-promise+signed+no-error keeps high. (dissenting reviewer argued medium/correctness since misdelivery is to user's own wallet)] | verified | #444 |
| F047 | 2 | swap | packages/sdk/src/actions/swap/providers/velodrome/encoding/routers/v2.ts:214-237 | medium | correctness | Velodrome universal-router has no native-ETH branch but quote sets msg.value=amountIn; native-in swaps misencode/strand ETH [P10 refined high/fund-loss->medium/correctness: real + CL path identical, but reachable only on Base Sepolia (testnet) universal/CL (mainnet hubs are v2 + handle native); dominant outcome is revert-with-refund, not mainnet fund-loss] | refines:self | none |
| F048 | 2 | swap | packages/sdk/src/actions/swap/providers/uniswap/encoding.ts:270-294 | medium | correctness | Exact-output swaps surface a meaningless amountOutMin but never expose the enforced amountInMaximum | new | #318 |
| F049 | 2 | swap | packages/sdk/src/actions/swap/providers/velodrome/encoding/helpers.ts:60-83 | medium | correctness | All Velodrome/Aerodrome quotes report priceImpact=0, disabling price-impact/illiquidity warnings | new | none |
| F050 | 2 | swap | packages/sdk/src/actions/swap/core/SwapProvider.ts:333-392 | medium | correctness | Permit2 sub-approval expiration independent of swap deadline, unbounded (30d), reused without reconciliation | new | #436 |
| F051 | 2 | swap | packages/sdk/src/actions/swap/providers/uniswap/encoding.ts:257-293 | low | correctness | V4 amount/min/max encoded into uint128 params with no <= maxUint128 bound check | new | none |
| F052 | 2 | swap | packages/sdk/src/actions/swap/namespaces/WalletSwapNamespace.ts:93-101 | low | correctness | requireQuoteForThisWallet recipient guard is a no-op for Uniswap quotes (calldata ignores recipient) | new | #437 |
| F053 | 2 | lend | packages/sdk/src/utils/approve.ts:85-90 | low | fund-loss | lend approvalMode='max' grants unbounded maxUint256 ERC-20 allowance directly to Aave Pool / Morpho vault (not Permit2) | new | #133 |
| F054 | 2 | borrow | packages/sdk/src/actions/borrow/namespaces/WalletBorrowNamespace.ts:207-247 | high | malicious-sign | Pre-built BorrowQuote dispatch validates only metadata, never the raw execution.transactions calldata bytes [P10 verified at high (three reviewers concur): validateQuoteForThisWallet checks only recipient/action/expiry/chain/allowlist metadata; dispatch signs execution.transactions verbatim; malicious approve(attacker,max) leg passes all guards; reachable via documented quote round-trip] | verified | none |
| F055 | 2 | borrow | packages/sdk/src/actions/borrow/core/quote.ts:54 | medium | correctness | safeCeilingLtv computed/surfaced as a safety value but never enforced on borrow/open/withdraw quotes | new | none |
| F056 | 2 | borrow | packages/sdk/src/actions/borrow/providers/aave/write.ts:111-135 | low | correctness | Aave native-ETH max collateral withdraw under-approves as aTokens accrue (undocumented sibling of repay tradeoff) | new | none |
| F057 | 2 | borrow | packages/sdk/src/actions/borrow/providers/morpho/repay.ts:33-47 | low | correctness | Morpho exact-assets repay does not clamp over-large amount to live debt; over-grants approval and reverts | new | #334 |
| F058 | 2 | wallet-core | packages/sdk/src/wallet/core/providers/smart/default/DefaultSmartWalletProvider.ts:168-184 | medium | correctness | getWallet drops caller-supplied nonce, so lazy deploy / CREATE2 fallback uses nonce 0 (wrong counterfactual address) | new | #98 |
| F059 | 2 | wallet-core | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:230-234,276-279 | low | info | appendAttributionSuffix targets uo.initCode, absent on EntryPoint v0.7 (v1.1); deployment-op attribution silently dropped | new | none |
| F060 | 2 | wallet-core | packages/sdk/src/wallet/core/utils/retryOnStaleRead.ts:20-39 | low | correctness | retryOnStaleRead swallows thrown read errors as stale; conflates RPC failure with signer-not-found in index resolution | new | none |
| F061 | 2 | wallet-core | packages/sdk/src/wallet/core/wallets/eoa/EOAWallet.ts:62-100 | low | info | EOAWallet.send/sendBatch do no recipient/value sanity on TransactionData before broadcasting (defense-in-depth) | new | #477 |
| F062 | 2 | wallet-hosted | packages/sdk/src/wallet/react/wallets/hosted/dynamic/utils/createSigner.ts:24-25 | low | info | Dynamic wallet.connector cast to DynamicWaasEVMConnector without an instanceof/capability guard before signRawMessage | new | none |
| F063 | 2 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:232-234,277-279,472-485 | medium | correctness | Attribution suffix appended to initCode (deployment calldata), mutating factory createAccount bytes; no version/shape assert | new | none |
| F064 | 2 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/utils/findSignerInArray.ts:17-29 | low | correctness | findSignerInArray silently ignores WebAuthn-only owner arrays; passkey-signing config picks index -1 and constructor throws | new | #163 |
| F065 | 2 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:594-598 | low | info | appendAttributionSuffix has no length assertion; 16-byte suffix validated in only one of two construction paths | new | none |
| F066 | 2 | core-services | packages/sdk/src/utils/validation.ts:176-180 | medium | correctness | validateRecipient silently no-ops on ENS names and any non-hex string — guards only the zero-address case | new | #437 |
| F067 | 2 | core-services | packages/sdk/src/utils/approve.ts:115-117 | low | malicious-sign | buildPermit2ApprovalTx computes Permit2 expiration with no positive/integer/uint48 bound (bad config => dead/rejected approval) | new | none |
| F068 | 2 | core-services | packages/sdk/src/services/nameservices/ens/EnsNamespace.ts:95-113 | low | info | ENS getName() reverse resolution returned without forward-confirmation — attacker-settable recipient label | new | #453 |
| F069 | 2 | core-services | packages/sdk/src/actions.ts:204-208 | low | correctness | Asset allow/block filter matches addresses chain-agnostically; blocking on one chain over-blocks a different asset sharing that address | new | #493 |
| (refines:F005) | 2 | swap | packages/sdk/src/actions/swap/core/SwapProvider.ts:286-298 | low | correctness | computeSlippageBounds rounds slippage to whole bps; sub-bp slippage coarsens to 0 protection and diverges display vs enforced | refines:F005 | #318 |
| (refines:F008) | 2 | lend | packages/sdk/src/actions/lend/core/LendProvider.ts:84-118 | low | correctness | openPosition never validates caller asset vs resolved market underlying while closePosition does (Aave native branch misroutes) [P10 refined high/fund-loss->low/correctness: native branch routes ETH to chain-canonical WETHGateway, depositor gets own aWETH (no attacker, recoverable, self-inflicted); intent/labeling mismatch not fund-loss. (the fund-safety angle is the base F008 max-allowance leg, verified high)] | refines:self | #334 |
| (refines:F008) | 2 | lend | packages/sdk/src/actions/lend/core/LendProvider.ts:90 | medium | correctness | openPosition deposit amount scaled by caller's asset decimals, not market underlying decimals (mis-scaled by 10^Δdecimals) | refines:F008 | #334 |
| (refines:F011) | 2 | lend | packages/sdk/src/actions/lend/providers/aave/AaveLendProvider.ts:56-84 | medium | correctness | _openPosition/_closePosition catch{} swallow precise named errors (incl. Morpho); masks validation vs transient RPC failure | refines:F011 | #474 |
| (refines:F013) | 2 | lend | packages/sdk/src/actions/lend/providers/morpho/MorphoLendProvider.ts:107-114 | medium | fund-loss | Morpho close cannot fully exit: assets-denominated withdraw, no maxUint256/redeem(shares); Aave also lacks max sentinel | refines:F013 | #209 |
| (refines:F013) | 2 | lend | packages/sdk/src/actions/lend/providers/aave/AaveLendProvider.ts:291-342 | low | correctness | Aave native close approves only params.amount of aWETH; accrued interest stranded, no max/all path | refines:F013 | #209 |
| (refines:F023) | 2 | wallet-core | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:76-80,196-207,122 | medium | correctness | Retrieved smart wallet signs with ownerIndex from caller signers order, not on-chain owner order; wrong-slot sig reverts [P10 refined high->medium alongside the other F023 loci: recoverable UserOp-rejection/DoS, no funds move] | refines:self | #163 |
| (refines:F028) | 2 | wallet-hosted | packages/sdk/src/wallet/node/providers/hosted/privy/PrivyHostedWalletProvider.ts:67,87-95 | medium | malicious-sign | Node Privy toActionsWallet getAddress-checks but createSigner skips it; neither reconciles address vs walletId | refines:F028 | none |
| (refines:F029) | 2 | wallet-hosted | packages/sdk/src/wallet/node/wallets/hosted/privy/PrivyWallet.ts:36,49-50,60-67 | high | fund-loss | Node Privy caller-supplied address used as lend onBehalfOf / smart-wallet owner, never reconciled with signing walletId | refines:F029 | none |
| (refines:F030) | 2 | wallet-hosted | packages/sdk/src/wallet/react/wallets/hosted/dynamic/utils/createSigner.ts:26-37 | low | correctness | Dynamic signer wires signTransaction/signMessage/signTypedData to WalletClient methods with non-CustomSource signatures | refines:F030 | none |
| (refines:F031) | 2 | wallet-hosted | packages/sdk/src/wallet/node/wallets/hosted/turnkey/utils/createSigner.ts:25-31 | low | correctness | Turnkey ethereumAddress override passed through unvalidated, becomes reported address with no cross-check vs signWith | refines:F031 | none |
| (refines:F037) | 2 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:229-236,274-281 | low | correctness | send/sendBatch forward only suffixed callData/initCode/paymaster, drop all prepared gas/fee fields; gas re-estimated on different bytes [P10 refined high->low: "gas re-estimated on different bytes" inverted — second prepare estimates against the SUFFIXED (executed) bytes, so no under-estimation; signed op self-consistent; reduces to wasted-prepare (low)] | refines:self | #456 |
| (refines:F039) | 2 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:390-422 | medium | fund-loss | removeSigner can remove the only LocalAccount this client can sign with, locking the wallet from this SDK instance | refines:F039 | #163 |
| (refines:F041) | 2 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:536-547 | medium | fund-loss | sendTokens takes JS number; parseUnits(number.toString()) loses precision / emits scientific notation on value-out path | refines:F041 | #379 |
| (refines:F042) | 2 | core-services | packages/sdk/src/utils/approve.ts:184-216 | low | correctness | getApprovalDeficit/buildApprovalTxIfNeeded approve the DEFICIT (ERC-20 approve sets not increments); zero callers, not exported — latent | refines:F042 | #133 |
| (refines:F043) | 2 | core-services | packages/sdk/src/services/nameservices/ens/utils.ts:20-25 | low | correctness | passthroughResolver returns recipient addresses un-checksummed (accepts strict:false) on default no-ENS swap recipient path | refines:F043 | #371 |
| F001 | 1 | swap | packages/sdk/src/actions/swap/core/SwapProvider.ts:164-167 | medium | correctness | getQuote() skips blocklist/slippage/recipient/same-asset validation that execute() enforces [P10 refined high->medium: validation asymmetry real (blocklist honored only by validateMarketAllowed which getQuote skips) but execute() re-runs validateSwapExecute on a passed-back quote, so bad quotes can't reach signing via execute(); residual = manual-calldata-extraction defense-in-depth. Slippage sub-claim separately over-severe (low)] | refines:self | #435 |
| F002 | 1 | swap | packages/sdk/src/actions/swap/providers/uniswap/UniswapSwapProvider.ts:92 | medium | correctness | Approval allowance check uses quote.recipient as token owner instead of executing wallet | new | #436 |
| F003 | 1 | swap | packages/sdk/src/actions/swap/providers/velodrome/encoding/routers/v2.ts:225 | medium | correctness | Universal-router and CL swap paths silently ignore caller-supplied recipient | new | #444 |
| F004 | 1 | swap | packages/sdk/src/actions/swap/providers/uniswap/UniswapSwapProvider.ts:172 | medium | fund-loss | Native-in exact-output swap sends placeholder 1-unit msg.value instead of maxAmountIn | new | none |
| F005 | 1 | swap | packages/sdk/src/actions/swap/providers/uniswap/encoding.ts:252 | low | correctness | Encoded minAmountOut/maxAmountIn recomputed independently of quote's reported slippage bounds | new | #318 |
| F006 | 1 | swap | packages/sdk/src/utils/validation.ts:51 | low | correctness | Same-asset guard compares only token symbols, not addresses/chain identity | new | #493 |
| F007 | 1 | swap | packages/sdk/src/actions/swap/namespaces/BaseSwapNamespace.ts:58 | low | correctness | Explicit-provider getQuote/getQuotes bypasses isMarketSupported eligibility filter | new | #435 |
| F008 | 1 | lend | packages/sdk/src/actions/lend/core/LendProvider.ts:84-118 | high | fund-loss | openPosition does not validate caller asset matches market underlying; Morpho approves wrong token [P10 verified at high: open/close asymmetry confirmed (close calls validateMarketAsset, open doesn't); residual max-allowance to vault on unintended token + Aave native-leg ETH misdirection sustains high; Morpho deposit-revert keeps short of critical] | verified | #334 |
| F009 | 1 | lend | packages/sdk/src/actions/lend/core/LendProvider.ts:84-118 | medium | correctness | No positive/non-zero amount validation on lend open/close amounts (swap sibling validates) | new | #303 |
| F010 | 1 | lend | packages/sdk/src/actions/lend/core/LendProvider.ts:234-257 | medium | correctness | marketBlocklist config field accepted and validated but never enforced on any lend path | new | #334 |
| F011 | 1 | lend | packages/sdk/src/actions/lend/providers/aave/AaveLendProvider.ts:79-83 | low | correctness | Catch-all blocks in provider open/close/getPosition flatten precise named errors | new | #474 |
| F012 | 1 | lend | packages/sdk/src/actions/lend/providers/morpho/sdk.ts:434-459 | low | correctness | Dead helper findBestVaultForAsset matches asset addresses chain-agnostically | new | none |
| F013 | 1 | lend | packages/sdk/src/actions/lend/providers/morpho/MorphoLendProvider.ts:107-114 | low | correctness | Morpho close uses assets-denominated withdraw with no full-balance/rounding handling | new | #209 |
| F014 | 1 | lend | packages/sdk/src/actions/lend/providers/aave/sdk.ts:108-118 | low | info | Aave getReserve builds fresh ethers JsonRpcProvider from default RPC, bypassing integrator client | new | #211 |
| F015 | 1 | borrow | packages/sdk/src/actions/borrow/core/internalParams.ts:129-140 | medium | correctness | Borrow amounts never validated as positive/non-zero, unlike swap sibling | new | none |
| F016 | 1 | borrow | packages/sdk/src/actions/borrow/core/BorrowProvider.ts:215-236 | low | correctness | getMarkets accepts public markets array that bypasses provider allowlist | new | #334 |
| F017 | 1 | borrow | packages/sdk/src/actions/borrow/providers/morpho/MorphoBorrowProvider.ts:79-88 | low | correctness | Morpho marketId<->marketParams integrity verified for allowlist but not blocklist | new | #334 |
| F018 | 1 | borrow | packages/sdk/src/actions/borrow/core/quote.ts:54 | low | correctness | safeCeilingLtv can go negative when healthBufferPct misconfigured > 1 | new | none |
| F019 | 1 | borrow | packages/sdk/src/actions/borrow/providers/aave/quote.ts:153-167 | low | info | Aave depositCollateral rejects max but Morpho accepts it — divergent sibling behavior | new | #493 |
| F020 | 1 | wallet-core | packages/sdk/src/wallet/core/wallets/eoa/EOAWallet.ts:62-73 | high | correctness | EOAWallet.send returns a reverted receipt as success (no status check) | verified | none |
| F021 | 1 | wallet-core | packages/sdk/src/wallet/core/wallets/eoa/EOAWallet.ts:90-100 | high | fund-loss | EOAWallet.sendBatch continues signing subsequent txs after a mid-batch revert [P10 verified at high: missing receipt.status check, smart sibling atomic+throws; worst end-state residual allowance + false-success, reversible/conditional but broad blast radius keeps high. (dissenting reviewer argued medium/correctness given exact-mode default)] | verified | none |
| F022 | 1 | wallet-core | packages/sdk/src/wallet/core/wallets/eoa/EOAWallet.ts:62-73 | low | correctness | EOAWallet.send/sendBatch accept any chainId without validating against configured chains | new | none |
| F023 | 1 | wallet-core | packages/sdk/src/wallet/core/providers/WalletProvider.ts:97-131 | medium | correctness | getSmartWallet with walletAddress but no signers pins ownerIndex to 0 / owners to [signer] [P10 verified at medium: reachable via getSmartWallet({walletAddress,signer}) (signers optional); wrong-slot UserOp rejected at validation, recoverable, no funds move] | verified | #163 |
| F024 | 1 | wallet-core | packages/sdk/src/wallet/core/wallets/abstract/Wallet.ts:90-100 | medium | correctness | Wallet base constructor attaches action namespaces but never initializes wallet address | new | #396 |
| F025 | 1 | wallet-core | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:494-499 | low | info | deploy() catch wraps all errors into SmartWalletDeploymentError and drops the receipt | new | none |
| F026 | 1 | wallet-core | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:178-188 | low | info | isValidAttributionSuffix has dead null-guard and misleading byte-count message | new | none |
| F027 | 1 | wallet-core | packages/sdk/src/wallet/core/wallets/eoa/EOAWallet.ts:90-100 | low | correctness | EOAWallet.sendBatch silently returns [] for empty input while executeTransactionBatch rejects it | new | none |
| F028 | 1 | wallet-hosted | packages/sdk/src/wallet/node/providers/hosted/privy/PrivyHostedWalletProvider.ts:87-95 | medium | malicious-sign | Node Privy createSigner skips the getAddress validation its sibling toActionsWallet applies | new | none |
| F029 | 1 | wallet-hosted | packages/sdk/src/wallet/node/wallets/hosted/privy/PrivyWallet.ts:36 | medium | correctness | Node PrivyWallet reports caller-supplied address never reconciled with signing walletId [P10 verified at medium: real missing-defensive-invariant (sibling React derives from signer), but reachable only via integrator misconfig (walletId+address read as a consistent pair in canonical flow), not external attacker] | verified | none |
| F030 | 1 | wallet-hosted | packages/sdk/src/wallet/react/wallets/hosted/dynamic/utils/createSigner.ts:28-37 | medium | correctness | Dynamic signer hand-rolls sign() with 0x hex-stripping on a divergent backend code path | new | none |
| F031 | 1 | wallet-hosted | packages/sdk/src/wallet/node/wallets/hosted/turnkey/utils/createSigner.ts:25-31 | low | correctness | Turnkey signer forwards ethereumAddress/signWith with no format validation | new | none |
| F032 | 1 | wallet-hosted | packages/sdk/src/wallet/node/providers/hosted/privy/__tests__/PrivyHostedWalletProvider.spec.ts:120-141 | low | info | Test gap: no coverage for createSigner address validation or address/walletId mismatch | new | #335 |
| F033 | 1 | wallet-hosted | packages/sdk/src/wallet/node/providers/hosted/registry/NodeHostedWalletProviderRegistry.ts:24-26 | low | info | Hosted provider validateOptions only checks truthiness of client handle, not its shape | new | none |
| F034 | 1 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:261-294,217-250 | high | correctness | send()/sendBatch() ignore UserOperation receipt.success — reverted action reported as success [P10 verified at high (two reviewers concur): sibling addSigner/removeSigner/deploy already check .success; silent-success across all 3 action namespaces, one-line precedented fix] | verified | none |
| F035 | 1 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:512-561 | medium | correctness | sendTokens() does not validate recipientAddress is a real address — only checks falsy | new | none |
| F036 | 1 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:522-525,536-561 | low | correctness | sendTokens() amount check amount<=0 does not reject NaN or non-finite values | new | #379 |
| F037 | 1 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:224-236,266-281 | low | correctness | send/sendBatch prepare the UserOperation twice; explicit prepare result discarded | new | #456 |
| F038 | 1 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:322-335 | low | correctness | addSigner WebAuthn path decodes publicKey as two bytes32 with no length validation | new | none |
| F039 | 1 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:390-422 | low | correctness | removeSigner does not cross-check caller-provided signerIndex actually holds the given signer | new | #163 |
| F040 | 1 | wallet-smart | packages/sdk/src/wallet/core/wallets/smart/default/DefaultSmartWallet.ts:573-587 | low | info | Deterministic wallet address derived from a single chain's RPC read (getSupportedChains()[0]) | new | #98 |
| F041 | 1 | core-services | packages/sdk/src/utils/assets.ts:17-19 | medium | correctness | parseDecimalAmount feeds number.toString() to parseUnits, producing scientific notation | new | #379 |
| F042 | 1 | core-services | packages/sdk/src/utils/approve.ts:184-216 | medium | correctness | getApprovalDeficit+buildApprovalTxIfNeeded approve the DEFICIT, but ERC-20 approve sets not increments | new | #133 |
| F043 | 1 | core-services | packages/sdk/src/services/nameservices/ens/utils.ts:43-47 | low | correctness | resolveAddress/EnsNamespace.getAddress accept and return non-checksummed hex recipients unchecked | new | none |
| F044 | 1 | core-services | packages/sdk/src/utils/serializers.ts:15-21 | low | correctness | serializeBigInt return type lies (bigint->string) and silently drops Map/Set/Date/undefined | new | #419 |
| F045 | 1 | core-services | packages/sdk/src/services/ChainManager.ts:180-185 | low | info | getTransportForChain falls back to viem default public RPC (http()) when no rpcUrls configured | new | #255 |
| (refines:F008) | 1 | core-services | packages/sdk/src/utils/validation.ts:27-49 | low | correctness | Amount positivity/finiteness exists for swap but absent on lend+borrow value paths (umbrella) [P10 refined high/fund-loss->low/correctness: NaN/Infinity throw in parseUnits, negative bigint throws in encodeAbiParameters (uint256) before signing; worst reachable is opaque throw or no-op zero tx; no fund-loss. Magnitude-corruption risk lives in decimals/wrong-token findings] | refines:self | #477 |
