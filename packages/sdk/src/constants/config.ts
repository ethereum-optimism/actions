/**
 * Default SDK configuration values
 */
export const DEFAULT_ACTIONS_CONFIG = {
  lend: {
    /** Default slippage tolerance for lending operations (in basis points: 50 = 0.5%) */
    defaultSlippage: 50,
  },
} as const
