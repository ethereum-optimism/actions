import {
  base,
  baseSepolia,
  bob,
  celo,
  fraxtal,
  ink,
  lisk,
  mainnet,
  metal,
  mode,
  optimism,
  optimismSepolia,
  sepolia,
  soneium,
  superseed,
  swell,
  unichain,
  unichainSepolia,
  worldchain,
} from 'viem/chains'

export const SUPPORTED_CHAIN_IDS = [
  mainnet.id,
  sepolia.id,
  optimism.id,
  optimismSepolia.id,
  base.id,
  baseSepolia.id,
  unichain.id,
  unichainSepolia.id,
  worldchain.id,
  bob.id,
  celo.id,
  fraxtal.id,
  ink.id,
  lisk.id,
  metal.id,
  mode.id,
  soneium.id,
  superseed.id,
  swell.id,
] as const

export type SupportedChainId = (typeof SUPPORTED_CHAIN_IDS)[number]
