/**
 * Morpho-specific types
 * @description Re-exports and custom types for Morpho lending provider
 */

// Re-export Morpho SDK types we use
export type {
  AccrualVault,
  IAccrualVault,
  IVault,
  Vault,
} from '@morpho-org/blue-sdk'
export type {
  IVaultMarketAllocation,
  VaultMarketAllocation,
} from '@morpho-org/blue-sdk'
